import swiftUI

var dato: [String] = "Hola Mundo"
var dato1 : [Int] = "1,2,3,4"

let dato = "Hola Mundo"
let dato1 = "1,2,3,4"

let DatoCons: [String] = "Hola Mundo"
let DatoCons: [Int] = "1,2,3,4"

var dato = "Hola Mundo"
var dato = "1,2,3,4"

var dato: [Stirng] = "Hola Mundo"
var Dato1: [Int] = "1,2,3,4"

var dato = x, y = 0, z = 0

var mensajedebienvenida: [String] = "Hola Mundo"

var rojo, verde, azul: Double = "Hola Mundo"

var dato = "Hola"
    "Hola" = "Buenas"

// Ahora Hola es "Buenas"

print(:Hola,Buenas:) 

let nombredelLenguaje = "Swift"
    nombredelLenguaje = "Proyecto"
        print(nombredelLenguaje)
        print(someValues, terminator: "")

//var dato = 20
//var dato1 = 15
    print("El dato actual es \(dato)")

/* Esto es un comentario */

let gato = "Hola", print(gato)
// Imprrime "gato"

let valorMinimo = UInt8.min // El valor minimo es igual 0 y de tipo UInt8
let valorMaximo = UInt8.max // El valor maximo es igual 1000 y de tipo UInt8

var dato: [Float] = 3,123,789
var dato: [Double] = 20

let dato = 42
let dato1 = 4,1111111
let dato2 = 3 + 1,222222

let enteroDecimal = 20
let enteroBinario = 0b10000 // 17 en notacion binaria
let enteroOctal = 0o21 // 17 en notacion octal
let enteroexaDecimal = 0x11 // 17 en notacion ExaDecimal

var dato = 10
    print("El numero es \(dato)")

let dato = ["Pedro";"Juan";"Oscar"]
    print(dato)

let valorMinimo = UInt8.min // El valor minimo es igual 0 y es de tipo UInt8
let valormaximo = UInt8.max // El valor maximo es igual 255 y es de tipo UInt8

let dato: [Int] = 42
let dato: [Double] = 4,341,123

let suma = 3 + 4,123

let doubleDecimal = 12,1785
let doubleExponente = 12,21875e1
let doubleExadecimal = 0xC.3p0

let doubleDecimal = 12,1785
let doubleExponente = 12,21875e1
let doubleExadecimal = 0xC.3p0

let doubeDecorado = 000123.456
let unMillon = 1_000_000
let pocoMasDeUnMillon = 1_000_000_000_1

let DoubleDecorado = 000123.456
let doubleUnMillon = 1_000_000
let pocoMasDeUnmillon = 1_000_000_000_1

let noPuedeSernegativo: Int8 = -1
let muyGrande: Int8 = Int8 +1

let dosMil: UInt16 = 2_000
let uno: UInt8 = 1
let dosMiluno: dosMil = Int8(1)

let tres = 3
let puntoUnoCuatroCincoCuatroUnoNuve = 0.145419
let pi = Double(tres) + tresPuntounocuatrocincocuatrounonueve

let piEntero = Int(pi)

let piEntero = Int(pi)

let tres = 3
let puntoUnoCuatroCincoCuatroUnoNueve = 0.145419
let pi = Double(tres) + puntoUnoCuatroCincoCuatroUnoNueve

let piEnetero = Int(pi)

typealias muestradelaudio = Inst16
var maximaAmplitudhallada = Int16.min

let naranjasAnaranjadas = true 
let naranjasAnaranjadas = false

typealias muestrasdelaudio = Int16
var maximaAmplitudAllada = Int16.min

if lasManzanasEstanBuenas {
    print(Las manzanas estan Buenas)
} else {
    print(No sirven las manzanas)
}

typealias muestrasdelaudio = Int16
var maximaAmplitudAllada = Int16.min

let i = 1 
    if {
        // Este ejemplo no se compilara poruqe hay un error
    }

let i == 1
    if {
        // Este ejemplo si se compilara sin problemas
    }

let errorHTTP404 = (404 Not Found)
// errorHHTP404 es de tipo (String y Int) por lo que es igual a (404, Not Found)

let (codigodelestado, mensajedelestado) = errorHTTP404
    print("El codigo del estado \(codigodelestado)")
    print("El mensaje del estado es \(mensajedelestado)")

let (dato, dato1) = (errorHTTP404)
    print("El uno es \(dato)")
    print("El dos es \(dato1)")

let (dato, _) = errorHTTP404
    print("El numero es \(dato)")

let (codigodelestado, mensajedelestado) = errorHTTP404
    print("El codigo del estado es \(errorHTTP404.0)")
    print("El mensaje del estado es \(errorHTTP404.1)")


let respuestaHTTP200 = (codigodelestado: 200, descripcion: OK)
    print("El codigo del estado es \(respuestaHTTP200.codigodelestado)")
    print("El codigo del estado es \(respuestaHTTP200.codigodelestado)")

let respuestaHTTP200 = (codigodelestado: 200, descripcion: OK)
    print("El codigo del estado es \(respuestaHTTP200.codigodelestado)")
    print("El codigo del estado es \(respuestaHTTP200.codigodelestado)")

let posiblenumero = "123"
let posiblenumero = Int(posiblenumero)

let posiblenumero = "456"
let posiblenumero = Int(posiblenumero)

// Se infiere que posiblenumero es de tipo Int? o (Int opcional)

var codigodelservidor: Int? = 404
// codigo del servidor contiene un valor real Int que es 404

var codigodelservidor = nil
// Ahora codigodelservidor no contiene ningun valor

var codigodelservidor: Int? = 404
    print(codigodelservidor)

var codigodelservidor = nil
    print(codigodelservidor)

var codigodelservidor = ("Pedro,Juan,Oscar")

var datos: String? = (Pedro,Juan,Oscar)
var datos = nil
    print(datos)

var datos: String?
// A datos se le asigna el nil automaticamente

var datos: Int? = 404
var datos = nil
    print(datos)

var datos: String? = (Pedro,Juan,Oscar)
var datos = nil

var datos: Int? = 1,2,3,4
var datoa = nil

if numero != nil {
    print("Numero tiene un valor entero")
}

if dato == nil {
    print("Dato no tiene un valor entero")
}

if numero != nil {
    print("NUmero no tiene ningun valor entero")
}

if numero == nil {
    print("Numero tiene valores enteros")
}

if numero != nil {
    print("Numero tiene un valor de \(numero!)")
}

if numero != nil {
    print("Numero tiene un valor de \(numero!)")
}

if let <#nombredelaconstante#> = <#algunopcional#> {
    <#instrucciones#>
}

if let <#nombredelacontante#> = <#algunopcional#> {
    <#instrucciones#>
}

if let <#datosborrados#> = <#datos!#> {
    <#datos > 20#>
}
if let <#datoslocos#> = <#datos!#> {
    <#datos < 50#> {
        print(datos)
    }
}

if let <#datosdelgrupo#> = <#datos!#> {
    <#datos#> < 50 {
        print(<#datos#>)
    }
}

if let numeroReal = Int(PosibleNumero) {
    print("La cadena '\(posibleNumero)") tiene un '\(numeroreal) {
    } else {
    print("La cadena \(posiblenumero)" no pudo convertirse en un entero)
    }
}

if let primerumero = Int(4) {
    if let segudoombre = Int(42) {
        if primerNumero < segundoNumero && segundoNumero< 100 {
            print("\(primerNumero) < \(segundoNumero) < 100")
        }
    }
}

if let primerNumero = Int(4) {
    if let segundoNumero = Int(42) {
        if primerNumero < segundoNumero && segundoNumero < 100 {
            print("\(primerNumero) < \(segundoNumero) < 100")
        }
    }
}

let numero: String! = ("Pedro","Juan","Oscar")
let dato: String? = ("Pedro","Juan","Oscar")

let dato: Int! = 1;2;3;4 
let dato: Int? = 1;2;3;4

if let primerNumero = Int(4) {
    if let segundoNumero = Int(42) {
        if let primerNumero < segundoNumero && segundoNumero < 100 {
            print("\(primerNumero) < \(segundoNumero) < 100")
        }
    }
}

let cadenaopcional: String? = "Posisble cadena opcional"
let cadenaopcional: String = "Una cadena opcional"
let cadenaopcional: String! = "Una posible cadena opcional extraida de forma explicita"
let cadenaopcional: String = "Cadena Asumida" 

let cadenaopcional: String? = "Posible cadena opcional"
let cadenaopcional: String = "Una cadena opcional"
let cadenaopcional: String! = "Una cadena opcional extraida de forma explicita"
let cadena opcional: String = "Cadena Asumida"

if let primernumero = Int(4) {
    if let segundonumero = Int(42) {
        if let primernumero < segundonumero && segundonumero < 100 {
            print("\(primernumero) < \(segundonumero) < 100")
        }
    }
}

let cadenaopcional = cadenaAsumida

if cadenaAsumida != nil {
    print(cadenaAsumida)
}

if let cadenaDefinitiva = cadenaAsumida {
    print(cadenaDefinitiva)
}

if let cadenaDefinitiva = cadenaAsumida {
    print(cadenaDefinitiva)
}

func puedeprodudiruerror() thorws {
    //Esta funcion puede que arroje o no un error
}

func puedeProducirunError() throws {
    //Esta funcion puede que arroje un error o no
}

func puedeProducirunError() throws {
    //Esta funcion puede producir un error o no
}

if let primerNumero = Int(4) {
    if let segundoNumero = Int(42) {
        if let primerNumero < segundoNumero && segundoNumero < 100 {
            print("\(primernumero) < \(segundoNumero) < 100")
        }
    }
}
func SePuedeProducirUnError() throws {
    //Esta funcion puede que tenga un error o no
}

do {
    try puedeArrojarUnError()
    // No se produjo un error
} catch {
    //Si se produjo un error
}

do {
    try puedeOcurrirUnError()
    // No se produjo un error
} catch {
    //Si se produjo un error
}

do {
    try PuedeArrojarUnError()
    //No se produce ningun error
} catch {
    //Si se produce un error
}

func PrepararUnSandwich() throws {
    // ...
}

do {
    try PrepararUnSandwich() {
        ComerSandwich()
} catch SandwichError.NohayplatosSucios() {
    lavarplatos()
} catch SandwichError.FaltanIngredientes(let ingredientes) {
    comprarProvisiones(ingredientes)
    }
}

func PrepararUnSandwich () throws {
    //...
}
do {
    try PrepararUnSandwich() {
        ComerSandwich()
} catch SandwichError.Nohayplatossucios() {
    LavarPlatos()
} catch SandwichError.FaltanIngredientes(let ingredientes) {
    ComprarProvisiones(ingredientes)
    }

}

func PrepararUnSandwich() throws {
    // ...
}
do {
    try PrepararUnSandwich() {
        ComerSandwich()
} catch SandwichError.NoHayPlatosSucios() {
    LavarPlatos()
} catch SandwichError.FaltanIngredientes(let ingredientes) {
    ComprarProvidiones(Ingredientes)
    }
}

func PrepararUnSandwich() throws {
    //...
}
do {
    try PrepararUnSandwich() {
        ComerSandwich()
} catch SandwichError.NohayPlatosSucios() {
    LavarPlatos()
} catch SandwichError.FaltanIngredientes(let ingredientes) {
    ComprarProvisiones(Ingredientes)
    }
}

func PrepararUnSandwich() throws {
    //...
}
do {
    try PrepararUNSandwich() {
        ComerSanWich()
} catch SandwichError.NoHayPlatosSucios() {
    LavarPlatos()
} catch SandwichError.FaltanIngredinetes(let ingredientes) {
    ComprarProviciones(Ingredientes)
    }
}

func PrepararUSandwich() throws {
    //..
}
do {
    try PrepararUnSandwich() {
        ComerSandwichs()
} catch SandwichError.NoHayPlatosSucios() {
    LavarPlatos()
} catch SandwichError.FaltanIngredientes(let Ingredientes) {
    ComprarProvisiones(Ingredientes)
    }
}

let edad = 3 
assert(edad >= 0 "La Edad de una persona no pueder ser menor que 0" )

let edad = 3
    assert(edad >= 0 "La edad de una persona no puede ser mayor que 0")

let edad = 3
    assert(edad >= 0 "La edad de una persona no puede ser menor que 0")

if edad > 10 {
    print("Puedes subir a la montana rusa y a la fortuna")    
} else if edad >= 0 {
    print("Puedes subir a la rueda de la fortuna")
} else {
    assertionFailure("La edad de una persona no puede ser menor que 0")
}

if edad > 10 {
    print("Puedes subir a la montana rusa y a la rueda de la fortuna")
} if else edad >= 0 {
    print("Puedes subir a la rueda de la fortuna")
} else {
    print("La edad edad de una persona no puede ser menor que 0")
}

//En la implementacion de un subscript
precondition(index > 0 "El indice debe ser mayor que 0")
assertionFailure(index > 0 "El indice no puede ser menor que 0")

let b = A
var A = b 
    A = B

let (x,y) = (1,2)
// x = 1 y y = 2

let a = b
var b = a 
    a = b

let (x,y) = (1,2)
// x = 1 y y = 2

if x = y {
    // Esto es falso porque x no devuelve ningun valor
}

let a = b 
var b = a 
    a = b

let (x,y) = (1,2)
// x = 1 y y = 2

if x = y {
    // Esto es falso porque x no ofrece ningun valor
}

"Hola, "+" Mundo" // Hola, MUndo
"Hola, "+" Mundo" // Hola, Mundo

let tres = 3
let menostres = -3
let masTres = -3

let tres = 3
let menosTres = -3
let masTres = -3

let menosSeis = -6
let TambienMenosSeis = +menosSeis

let MenosSeis = -6
let TambienMenosSeis = +MenosSeis

var a = 1
    a += 2

var a = 1
    a += 2

var a = 1 
    a += 2
// Ahora a es igual a 3

a += 2 // a = a + 2

a += 2 // a = a + 2

let nombre = "mundo"
    if nombre == "mundo" {
        print("Hola,Mundo")
    } else {
        print("Lo siento, \(nombre), no te reconozco")
    }

let nombre = "mundo"
    if nombre == "mundo" {
        print("Hola,Mundo")
    } else {
        print("Lo siento, \(nombre), pero no te reconozco")
    }

let nombre = "mundo"
    if nombre == "mundo" {
        print("Hola,Mundo")
    } else {
        print("Lo siento, \(nombre), pero no te reconozco")
    }

let a = b
var b = a 
    a = B

let nombre = "Mundo"
    if nombre == "MUndo" {
        print("Hola,Mundo")
    } else {
        print("LO siento \(nombre) pero no te conozco")
    }

a = 1
b += 1
// Ahora es igual 3

let (x,y) = (1,2)
// x = 1 y = 2

if x = y {
    // Esto es falso porque y no devuelve ningun valor
}

let tres = 3
let menosTres = -3
let masTres = -3

let menosSeis = -6
let masMenosSeis = +MenosSeis // tambien seis = -6

var a = 1
var a += 2
// Ahora a = 3

let b = a +=2 // Los aperadores de asignacion compuesta no devuelven un valor

let nombre = "Mundo"
    if nombre == "Mundo" {
        print("Lo siento \(nombre) no te reconozco")
    }

("1, pantera") < ("2, manzanas") // Es true porque 1 < 2
("3, manzanas") < ("3, platanos") // Es true porque 3 = 3
("4, perros") == ("4, perros") // Es true porque 4 = 4 y perro es igual a perro

("azul, -1") < ("morado, 1") //ok, evalua true
("azul, true") < ("rojo, false") // Error porque < no comparar valores Bool

let numero = 5
    if numero > 10 {
        print("numero es 7")
    } else {
        print("numero es 4")
    }

let alturaContenido = 40
let tieneEncabezado = true
let alturaFila = alturaContenido + (tieneEncabezado ? 50 : 20) 
//alturaFila es igual 90

let alturaContenido = 40
let tieneEncabezado = true
let alturaFila = alturaContenido + (tieneEncabezado ? 50 : 20)

if tieneEncabezado {
    alturaContenido + 50
} else {
    alturaFila + 20
}

if tieneEncabezado {
    alturaContenido + 50
} else {
    alturaFila + 20
}

a != nil ? a! :b

let nombreDeColorPredeterminado = "rojo"
let nombreDeColorDefinidoPorUsuario = String? // Por defecto te pone nil

let nombreDeColorPredeterminado = "rojo"
let nombreDeColorDefinidoPorUsuario = String? // Por defecto te pone nil

var nombredecolorausar = nombredecolorpredeterminado ?? nombredecolordefinidoporusuario
//nombredecolordefinidoporusuario es nil por lo que se le pone el valor de rojo

var nombreDeColorAUsar = nombredecolorpredeterminado ?? nombredecolordefinidoporusuario

let nombredecolorpredeterminado = "rojo"
let nombredecolordefinidoporusuario = String? //Por defecto se pone nil

let nombreDeColorPredeterminado = "verde"
var nombreDeColorAusar = nombreDeColorPredeterminado ?? nombreDeColorIndefinidoPorUsuario
//nombreDeColorIndefinidoPorusuario es nil por lo que se le asigna a nombreDeColorPredetermindo el valor "verde"

if tieneEncabezado {
    alturaFila = 50
} else {
    alturacontenido = 20
}

let nombreDeColorDefinidoPorusuario = "verde"
var nombreDeColorAusar = nombredecolorinfinidoporusuario ?? nombredecolorpredeterminado
// NombreDeColorPredeterminado no es nil, por lo que a NombreDeColorAUsar si se le asigna el "verde"

let NombreDeColorIndefinidoPorUsuario = "verde"
var NombreDeColorAUsar = NombreDeColorIndefinidoPorUsuario ?? NombreDeColorPredeterminado

for 1...5 in indice {
    print("\(indice) multiplicado por 5 \(indice *5)")
}

for indice in 1...5 {
    print("\(indice) multiplicado por 5 \(indice * 5)")
}
 
for indice in 1...5 {
    print("\(indice) multiplicado por 5 \(indice * 5)")
}

// 1 multiplicado por 5 es 5
// 2 multiplicado por 5 es 10
// 3 multiplicado por 5 es 15

let nombres = ["Pedro","Juan","Oscar"]
let conteo = nombres.count 
    for i in 0..<conteo {
        print("La persona #\(i + 1) se llama \(nombres[i])")
    }

let nombres = ["Pedro","Juan","Oscar","Julio"]
let conteo = nombre.count
    for i in 0..<conteo {
        print("La persona #\(i + 2) se llama \(nombres[i])")
    }

let nombres = ["Pedro","Juan","Oscar","Julio"]
let conteo = nombres.count
    for i in nombre.count {
        print("La persona #\(i + 2) se llama \(nombres[i])")
    }

let nombres = ["Pedro","Juan","Oscar","Julio"]
let nombre = ["Omar"]
    for nombre in nombres[2...] {
        print(nombre)
    }

    for nombre in nombres[...2] {
        print(nombre)
    }

// Imprimir Pedro
// Imprimir Juan
// Imprimir Oscar

let nombres = ["Pedro","Juan","Oscar"]
let nombre = ["Omar"]
    for nombre in nombres[...2] {
        print(nombre)
    }

    for nombre in nombres[2...] {
        print(nombre)
    }

for nombre in nombres[..<2] {
    print(nombre)
}

for nombre in nombres[..<2] {
    print(nombre)
}

let rango = ...5
    rango.contains(7) //False
    rango.contrains(4) //True
    rango.contains(-1) //True

let EntradaPermitida = false
    if !EntradaPermitida {
        print("Acceso Denegado")
    }

let EntadaPermitida = false
    if !EntradaPermitida {
        print("Acceso Denegado")
    }

let ingresoCodigoDePuerta = false
let aprueboEscanerDeRutina = true
    if ingresoCodigoPuerta && aprueboEcanerDeRutina {
        print("¡Bienvenida!")
    } else {
        print("Acceso Denegado")
    }

let ingresoCodigoPuerta = false
let aprueboCodigoEscaner = true
    if ingresoCodigoPuerta && aprueboCodigoEscaner {
        print("¡Bienvenida!")
    } else {
        print("Acceso Dnegado")
    }


let ingresoCodigoPuerta = false
let aprueboCodigoEscaner = false
    if ingresoCodigoPuerta && aprueboCodigoEscaner {
        print("¡Bienvenida!")
    } else {
        print("Acceso Denegado")
    }

let tienesLaLLavedelapuerta = false
let conocelallavemaestra = true
    if conocelallavemaestra || tieneslallavedelapuerta {
        print("¡Bienvenida!")
    } else {
        print("Acceso Denegado")
    }

let tienesLaLlaveDeLaPuerta = false
let conocelallavemaestra = true
    if conoceLaLaaveMaestra || tinesLaLlaveDeLaPuerta {
        print("¡Bienvenida")
    } else {
        print("Acceso Denegado")
    }

let tienesLaLlaveDeLaPuerta = false
let conoceLaLlaveMaestra = true
    if conecelallavemaestra || tieneslallavedelapuerta {
        print("¡Bienvenida!")
    } else {
        print("Acceso Denegado")
    }

let nombres = ["Pedro","Juan","Oscar","Pedro"]
let nombre = ["Omar"]
    for nombres in nombre[...2] {
        print(nombre)
    }

let tienesLasllavesdelapuerta = false
let conocelallavemaestra = true
    if ingresocodigodepuerta && tieneslallavedelapuerta || conocelallavemaestra {
        print("¡Bienvenida!")
    } else {
        print("Acceso Denegado")
    }

    if ingresocodigodepuerta && tineslallavedelapuerta || conocelallavemaestra {
        print("¡BIenvenida!")
    } else {
        print("Acceso Denegado")
    }

    var a = b 
    let b = a 
        a = B

    if ingresocodigodepuerta && tieneslallavedelapuerta || conoceslallavemaestra || apruebouncodigoescaner {
        print("¡Bienvenida!")
    } else {
        print("acceso Denegado")
    }

    var a = b 
    let b = a 
        a = b 

    let nombres = ["Pedro","Juan","Oscar","Julio"]
    let nombre = ["Omar"]
        for nombre in nombre[...2] {
            print(Nombre)
        } 

        for nombre in nombres[2...] {
            print(nombre)
        }

var nombre: [String] = ("Pedro","Juan","Oscar","Julio")
var numeros: [Int] = 1,2,3,4

let nombre: [String] = ("Pedro","Juan","Oscar","Julio")
let numeros: [Int] = 1,2,3,4

var nombre: [String] = ("Pedro","Julio","Juan","Oscar")
var numeros: [Int] = 1,2,3,4

let nombre: [String] = ("Pedro","Juan","Pedro","Julio")
let nombre.count()

var cadena = "Esto es una cadena que se puede modificar"
let cadena = "Esto es una cadena que no se puede modificar"

let cadena = 1,2,3,4 // Esta es una cadena de tipo entero porque inicio con numeros
let cadena = "Esto es una cadena de tipo String"

let cadena = "Esto esta una cadena"
let cadena = """
        Aqui se pueden escribir todas las lienas que sean
    """

let cadena = "Esta es una cadena de una sola linea"
let cadenaConVariasLineas = """
    Oro parece, plata no es y el que lo adivine bien bobo es 
    """

let citaModificada = """
    Oro paarece, plata no es, \ y el que no lo advine bien boboi es
    hola \ que parece 
    """

let saltoDeLinea = """
    Esto es una cadena que comienza con salto de linea
    Termina con un salto de linea
    """

let cadena = """
    Esto es una cadena de una sola linea
        Esto de una cadena de una sola linea
    Esto es una cadena de una sola linea
        Esto es una cadena de una sola linea 
    """

let cadena = """
    Esto es una cadena de varias lineas
        Esto es una cadena de varias lineas
    Esto es una cadena de varias lineas
        Esto es una cadena de varias lineas
    """
var nombre: [String] = ("Pedro","Juan","Julio")
var nombre: [Int] = 1,2,3,4

let nombre: [String] = ("Pedro","Juan","Julio")
let numeros: [Int] = 1,2,3,4

var nombre = "Esto es una linea que se puede modificar"
let nombre = "Esto es una linea que no se puede modificar poer ser una constante"

let cadena = """
    Esto es una cadena de varias lineas
        Esto es una cadena de varias lineas
    Esto es una cadena de varias lineas
        Esto es una cadena de varias lineas
    """

let nombre = "\"Hola MUndo\" 
let nombre = "\Hola Mundo\"

let signoDolar = "\{u24}" // $ Escalar Unicode U+0024
let corazonNegro = "\{2665}" // Escarlar Unicode U+2665
let corazonBrinllante = "\u{IF496}" // Escalar Unicode U+IF496

let signoDolar = "\{u24}"
let corazonNegro = "\{2665}"
let corazonBrillante = "\u{IF496}"

let sigoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{IF496}"

let tresComillas = """
    Escapar la primera comillas \"""
        Escapando las tres comillas \"\"\"
    """

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{IF496}"

let tresComillas = """
    Escapar de la primera comilla \"""
        Escapando de las tres comillas \"\"\"
    """

let tresComillas = """
    Escapar de la primera comilla \"""
        Escapando de las tres comillas \"\"\"
    """

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{IF496}"

let trescomillasDoblesMas = #"""
    Aqui hay tres comillas dobles mas: """
    """#

let tresComillasDoblesMas = #"""
    Aqui hay tres comillas dobles mas: """
    """#

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{IF296}"

let trescomillasDoblesMas = #"""
    Aqui hay tres comillas dobles mas : """
    """#

var cadenaVacia = Stirng()
    cadenaVacia.count {
        print(cadenaVacia)
    }

var cadenaVacia = ""
    print(cadenaVacia)

let cadenaVacia = String()
    print(cadenaVacia)

let cadenaVacia = ""
    print(cadena)


var cadenaVacia = String()
    if cadenaVacia.isEmpty {
        print("Nada que ver por aqui")
    }

let cadenaVacia = String()
    if cadenaVacia.isEmpty {
        print("Nada que ver por aqui")
    }

var nombre: [String] = ("Pedro","Juan","Julio")
    print("nombre")

var numeros: [Int] = 1,2,3,4
    print("numeros")

var datos = """
    Esto son varias lineas
        Esto son varias lineas
    Esto son varias lineas
        Esto son varias lineas
    """
let signoDolar = "\{u24}"
let corazon rojo = "\{2665}"
let corazonBrillante = "\u{if496}"

let copaVacia = [String]()
    if copaVacia.isEmpty {
        print("Aqui no hay nada")
    }

let copaVacia = [String]()
    if copaVacia.isEmty {
        print("Aqui no hay nada")
    }

var cadena = "Caballo"
    cadena += "Y el carruaje" // Ahora esto sigifica que al compilar sale Caballo y el carruaje
        print(cadena)

let cadena = "Caballo"
    cadena += "Y el carruaje" // Esto es un error de compilacion porque las constantes no se modificar 
        print(cadena)

var cadena = "Caballo"
    cadena += "Y el carruaje" // Cuando imprimes sale Caballo y el Carruaje

let cadena = "Caballo"
    cadena += "Y el Carruaje" // Esto es un error de compilacion porque las constantes no se pueden modificar

for character in "¡Perro!" {
    //P
    //e
    //r
    //r
    //o
}

var perro = "Character"
    for character in "¡Perro!" {
        //¡
        //P
        //e
        //r
        //r
        //o
        //!
    }


let signosDeexclamacion: Character = ¡
let signosDeExclamacion: Character = ¡

let caracteresGato: [Character] = ("¡","A","B","C","D","E","!")
let cadenaGato = String(caracteresGato)
    print(caracteresGato)

var cadena = "Caballo"
    cadena += "Y el carruaje" // Ahora Caballo y el Carruaje no hay error de compilacion

let cadena = "Caballo"
    cadena += "Y el Carruaje" // Aqui hay un error de compilacion porque las constantes no se pueden madificar

var cadenaVacia = String()
    if cadenaVacia {
        print(cadenaVacia)
    }

var datos = """
    Esto es una variable de una sola linea
        Esto es una variable de una sola linea
    Esto es una variable de una sola linea
        Esto es una variable de una sola linea
    """

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{if496}"

var nombres: [String] = ("Pedro","Juan","Oscar")
let numeros: [Int] = 1,2,3,4

let cadenaVacia = String()
    if cadenaVacia.isEmpty {
        print(cadenaVacia)
    }

var cadena = """
    Esta cadena es de una sola linea
        Esta cadena es de una sola linea
    Esta cadena es de una sola linea
        Esta cadena es de una sola liena
    """

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{if496}"

var cadena = "caballos"
    cadena += "Y el Carruaje" //Caballo y el carrueje este no da error de compilacion y por lo tanto se puede modificar

let cadena = "caballos"
    cadena += "Y el carruaje" // caballos y el carruaje da un error de compilacion porque las constantes no se pueden modificar

let cadenaGato: [Character] = ("¡","A","B","D","E","!")
let cadenaGato = String(cadenaGato)
    print(cadenaGato)

let cadenaGato: [Character] = ("¡","A","B","C","D","E","!")
let cadenaGato = String(cadenaGato)
    print(cadenaGato)

var Perro = "Character"
    for character in ¡Perro! {
        // ¡
        // P
        // E
        // R
        // R
        // O
        // !
    }

var nombres: [String] = ("Pedro","Jose","Carlos")
let numeros: [Int] = 1,2,3,4,5

let cadenaVacia = String()
    if cadenaVacia.isEmpty {
        print(cadenaVacia)
    }

let nombres = """
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    """

let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{IFG496}"

var cadena = "Carruaje"
    cadena += "Y el carruaje" //Caballosy el Carruaje no da error de compilacion porque la variable se puede modificar

let cadena = "Carruaje"
    cadena += "Y el carruaje" //Caballos y el Carruaje da un error de compilacion porque las constantes no se pueden modificar

let cadenaGato: [Character] = ("¡","A","B","C","D","E","F","!")
let cadenaGato = String(cadenaGato)
    print(cadenaGato)

let cadenaGato: [Character] = ("¡","A","B","C","D","E","!")
let cadenaGato = String(cadenaGato)
    print(cadenaGato)

var perro = "Character"
    for cadena in ¡Perro! {
        //¡
        //P
        //e
        //r
        //r
        //o
        //!
    }
   
var perro = "Character"
    for charactar in ¡Perro! {
        //¡
        //P
        //e
        //r
        //r
        //o
        //!
    }

let palabras = "\" Hola mundo \"
let palabras = "\" Hola mundo \"

let cadena1 = "Hola"
let cadena2 = "Mundo"
var datos = cadena1 + cadena2
    print(datos) //Ahora datos es igual cadena1 + cadena2

var datos = "Nombres"
    datos += Nombres //Ahora datos es igual a nombres

let cadena1 = "Hola"
let cadena2 = "Mundo"
var datos = cadena1 + cadena2 //Ahora datos es igual a cadena1 mas cadena 2
    print(datos)

var datos = "Buenos Dias"
    datos += cadena2 //Ahora datos es igual Buenos Dias mundo

let cadena1 = "Hola"
let cadena2 = "Mundo"
var datos = cadena1 + cadena2

var datos = "Buenos Dias"
    datos += cadena2 // Imprime datos es igual Buenos Dias Mundo

let punto: Character = "."
    saludo.append(punto)
// Ahora punto es igual a Buenos Dias 

let punto: Character = "."
    saludo.apeend(punto)
//Ahora punto es igual a Buenos Dias 

let malComienzo = """
    Uno
    Dos 
    Tres 
"""

let Final = """
    Uno
    Dos
    Tres
"""
print("malComienzo + Final")

let buenComiendo = """
    Uno
    Dos
    Tres
"""
print(buenComienzo + Final)

let cadena1 = "Hola"
let cadena2 = "Mundo"
var datos = cadena1 + cadena2
     print(datos)

var datos = "Buenos Dias"
    datos += cadena2
       print(datos)

var datos: Character = "."
    datos.append(punto) //Ahora punto es igual a Buenos
        print(datos)

let cadenaVacia = String()
    if cadenaVacia.isEmpty {
        print(cadenaVacia)
    }

let nombres = """
    Esto es una cadena de una sola linea
        Esto es una cadena de una sola linea
    Esto es una cadena de una sola linea
        Esto es una cadena de una sola linea
"""

let signoDolar = "\"{u24}"
let corazonRojo = "\"{2665}"
let corazonBrillante = "\"u{IF496}"

let nombre = "\" Esto es una sola linea "\"

let malComienzo = """
    uno
    dos
    tres
"""
let final = """
    uno
    dos
    tres
"""
print(malComienzo + final)

let buenComienzo = """
    uno
    dos
    tres
"""
print(buenComienzo + final)
//Imprime 1 lineas
//Imprime 2 lineas
//Imprime 3 lineas

let multiplicador = 3
let mensaje = "\(multiplicador) por 2.5 es \(Double(Multiplicador) * 2.5)"
// mensaje es "3 x 2.5 es 7.5"

let multiplicador = 3 
let mensaje = "\(multiplicador) por 3 es \(Double(Multiplicador) * 2.5)"

var nombres: [String] = ("Pedro","Juan","Oscar")
let numeros: [Int] = 1,2,3,4

let datos = String()
    if datos.isEmpty {
        print(datos)
    }

let numeros = """
    Esta es una escritura de una sola linea 
        Esta es una escritura de una sola linea
    Esta es una escritura de una sola linea
        Esta es una escritura de una sola linea
"""

let signoDolar = "\"{u24}"\"
let corazonRojo = "\"{2665}"\"
let corazonBrillante = "\"u{IF496}"\"

let datos = "\" Hola esto es una escritura de una sola linea "\"

let malComienzo = """
    uno
    dos
    tres 
"""
let final = """
    uno
    dos 
    tres 
"""
print("malComienzo + final")

let buenComienzo = """
    unos
    dos
    tres
"""
print("buenComienzo + final")

var nombres: [String] = ("Pedro","Juan","Oscar")
let nombres: [Int] = 1,2,3,4

let nombres = String()
    if nombre.isEmpty {
        print(nombre)
    }

let nombres = """
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    """

let nombres = "\" Esta es una cadena de una sola linea "\"

let signoDolar = "\"{u24}"
let corazonRojo = "\"{2665}"
let corazonBrillante = "\"u{if496}"

let malComienzo = """
    Uno
    dos
    tres
"""
let final = """
    uno
    dos
    tres
"""
print("malComienzo + final")

let buenComienzo = """
    uno
    dos
    tres
"""
print("buenComienzo + final")

var datos = "Caballos"
    datos += "Y el carruaje" //Ahora datos es Caballos y el Carruaje 
    print("datos")

let datos = "Caballos"
    datos += "Y el carruaje" // Esto resulta un error en el tiempo de compilacion porque las constantes no se pueden modificar 

var Perro = "Character"
    for character in ¡Perro! {
        // ¡
        // P
        // e
        // r
        // r
        // o
        // !
    }

var Perro = "Character"
    for character in ¡Perro! {
        //¡
        //P
        //e
        //r
        //r
        //o
        //!
    }

let nombre: Character = "!"
    print("nombre")

let nombre: Character = "!"

let nombre: [Character] = ["¡","N","o","m","b","r","e","!"]
let nombre = String(nombre)

let nombre = [Character] = ["¡","N","o","m","b","r","e","!"]
let nombre = String(Nombre)
    print(Nombre)

let datos = [Character] = ["¡","d","a","t","o","s","!"]
let datos = String(datos)
    print(datos)

let cadena1 = "Buenos"
let cadena2 = "Dias"
    datos = cadena1 + cadena2
        print(datos)

let cadena1 = "Buenos"
let cadena2 = "Dias"
    datos = cadena1 + cadena2
        print(datos)

var nombre = "Deletrea la palabra"
    nombre += cadena2 //Ahora nombre es deletrea la palabra Dias
        print(nombre)

let cadena1 = "Buenos"
let cadena2 = "Dias"
    var datos = cadena1 + cadena2

var nombre = "Deletra la Palabra"
    nombre += cadena2 //Ahora nombre es Deletrea la palabra dias 
        print(nombre)

let nombre = [Character] = ["¡","n","o","m,","b","r","e","!"]
    print(nombre)

let multiplicador = 3
let mensaje = "\(multiplicador) por 2.5 es \(Double(multiplicador) * 2.5")"
//Mensaje es 3 x 2.5 es igual a 7.5

let multiplicador = 3
let mensaje = "\(multiplicador) por 2.5 es \(Double(multiplicador) * 2.5)"

let datos = 3
let mensaje = "\(multiplicador) por 2.5 es \(Double(multiplicador) * 2.5"
    print(#"Escribe una cadena interpolada en Swift usando \(multiplicador). "#)

let datos = 3
let mensaje = "\(datos) por 2.5 es \(Double(datos) * 2.5"
print(#"Escribe una cadena interpolada de Swift usando \(datos)."#)

let cadena1 = 6
let cadena2 = 7
print(#"6 * 7 es \#(6 * 7). "#)
print(#" 6 * 7 es \#(6 * 7). #")
// Imprime 6 por 7 es 42


let eTildada = "\u{E9}"
let eTildadaCombinada: Character = "\u{65}\u{301}"
//Imprime etiltada es e y eTildadaCombinada es e

let eTildada = "\u{E9}"
let eTildadaCombinada: Character = "\u{65}\u{301}"
//Imprime eTildada es igual e y eTildadaComabinada es igual e 

var nombres: [String] = ("Pedro","Juan","Oscar")
var numeros: [Int] = 1,2,3,4

let nombres = String()
    if nombre.isEomty {
        print(nombre)
    }

let nombre = """
    Esta es una cadena de una sola linea
        Esto es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esto 11es una cadena de una sola liena
    """
let signoDolar = "\{u24}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{if496}"

var Perro = "Character"
    for character in ¡Perro! {
        //¡
        //P
        //e
        //r
        //r
        //o
        //!
    }

let nombre = [Character] = ("¡","N","o","m","b","r","e","!")

let nombre = 3
    print("\(nombre) * 3 \(nombre) * 2,5")

//Imprime 3 por 2,5 es 7,5

let eTildada = "\{uE9}"
let eTildadaCombinada = "\u{65}\u{301}"

let eTildada = "\{u59}"
let eTildadaCombinada = "\u{65}\u[301]"

let eTildada = "\{u59}"
let eTildadaCombinada = "\u{65}\u{301}"

let eTildada = "\{u59}"
let eTildadaCombinada = "\u{65}\u{301}"
//Imprime eTildada es e y eTildadaCombinada es e

let nombres = ("Pedro","Juan","Oscar","Julio")
    print("Nombre tiene \(nombres.count) caracteres")
//Imprime nombres tiene 34 caracteres

let animales = ("Gato","Perro","Caballo","Iguana")
    print("animales tiene \(animales.count) caracteres")
//Imprime "animales" tiene 34 caracteres

var nombres = ("Pedro","Juan","Oscar","Julio")
    nombres.count {
        print(nombres)
    }

var nombres = ("Pedro","Juan","Pedro","Oscar")
    print("nombres tiene \(nombres) caracteres")
    //Imrpime nombres tiene 34 caracteres

var palabra = "cafe"
    print("El numero de caracteres en  \(palabra) es \(palabra.count)")

var nombres = "Pedro"
    print("El numero de caracteres en \(nombres) es \(Nombres.count)")

var palabra = "cafe"
    print("El numero de caracteres en \(palabra) es \(palabra.count)")

    palabra += "\{u301}"
        print("El numero de caracteres en \(palabra) es \(palabra.count)")
        // Imprime "cafe" tiene 4 caracteres

var palabra = "cafe"
    print("El numero de caracteres en \(palabra) es \(palabra.count)")

    palabra += "\u{301}"
        print("El numero de caracteres en \(palabra) es \(palabra.count)")
    // Imprime El numero de caracteres en "cafe" es 4

var palabra = "cafe"
    print("El numero de caracteres en \(palabra) es \(palabra.count)")

    palabra += "\u{301}"
        print("El numero de caracteres en \(palabra) es \(palabra.count)")

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
// Imprime ¡

saludo[saludo.index(before: saludo.endIndex)]

let saludos = "Buenos Dias"
    saludos[saludos.startIndex]

saludos[saludos.index(before: saludos.endIndex)]

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.endIndex]
    saludo[saludo.index(after: saludos.endIndex)]
    saludo[saludo.index(before: saludo.startIndex)]

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.endIndex]
    saludo[saludo.index(before: saludo.endIndex)]
    saludo[saludo.index(after: saludo.startIndex)]

let index = saludo.index(saludo.startIndex, offsetBy: 8)
    saludo[index]

let index = saludo.index(saludo.startIndex, offSetby: 8)
    saludo[index]

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.endIndex]
    saludo[saludo.index(before: saludo.endIndex)]
    saludo[saludo.index(after: saludo.startIndex)]

let index = saludo.index(saludo.index, offSetby: 8)
    for index in saludo.indices {
        print("\(saludo[index]) ",Terminator: "")
    }

let saludo = "Buenos Dias"
    for index in saludo.indices {
        print("\(saludo[index]) ",Terminator: 8)
    } 

let saludo = "Buenos Dias"
    for index in saludo.indices {
        print("\(saludo[index]) ",Terminator: 8)
    }

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.index(before: endIndex)]
        for index in indices {
            print("\(saludo[index]) ",Terminator: "")
        }

let index = saludo.index(saludo.index, offsetby: 8)
    for index in indices {
        print("\(saludo[index]) ",Terminator: "")
    }

let index = saludo.index(saludo.index, offSetby: 8)
    for index in indices {
        print("\(saludo[index]) ",Terminator: "")
    }

var saludoBienvenida = "Buenos Dias"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    //Imrprime Ahora saludoBienvenida es igual a Buenos Dias

var nombre = "Pedro"
    nombre.insert("!", at: nombre.endIndex)
    nombre.insert(contentOf: "Omar", at: nombre.index(before: nombre.endIndex))

var nombre = "Pedro"
    nombre.insert("!", at: nombre.index)
    nombre.insert(contentOf: "Omar", at: nombre.index(before: nombre.endIndex))
    nombre.remove(at: nombre.index(before: nombre.endIndex))

var nombre: [String] = ("Pedro","Juan","Julio")
var numeros: [Int] = 1,2,3,4,5 

let nombre: [String] = ("Pedro","Juan","Oscar")

let nombres = """
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esta es una cadena de un sola linea
    """

let nombre = [Character] = ("¡","n","o","m","b","r","e","!")

let nombre = "Nombre"
    for character in ¡nombre! {
        //¡
        //n
        //o
        //m
        //b
        //r
        //e
        //!

    }

let signoDolar = "\{u34}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{if496}"

let nombres = ("Pedro","Juan","Oscar")
    print("\(nombres) tiene \(nombres.count) caracteres")
    //Imprime nombres tiene 14 caracteres

var palabra = "cafe"
    print("El numero de caracteres de \(palabra) es \(palabra.count)")
    //Imprime "palabra" tiene 4 caracteres

let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.index(before: saludo.endIndex)]
    saludo[saludo.index(after: saludo.startIndex)]

let index = "saludo[saludo.index(saludo.startIndex, offSetby: 8)]"
    saludo[index]

let index = "saludo[saludo.index(saludo.startIndex, offSetby: 8)]"
    for index in indices {
        print("\(saludo[index]) ", terminator: "")
    }

let index = "indices"
    for index in indices {
        print("\(saludo[index]) ",terminator: "")
    }


let saludo = "Buenos Dias"
    saludo[saludo.startIndex]
    saludo[saludo.endIndex]
    saludo[saludo.index(before: saludo.endIndex)]
    saludo[saludo.index(after: saludo.startIndex)]

let index = "saludo[saludo.index(saludo.startIndex, offSetby: 8)]
    saludo[index]

let index = "indices"
    for index in indices {
        print("\(saludo[index]) ", terminator: "")
    }

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Tardes", at: saludoBienvenida.endIndex)

let saludBienvenida = "Buenas"
    saludBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludBienvenida.insert(contentOf: "Tardes", at: saludBienvenida.endIndex)

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Dias", at: saludoBienvenida.endIndex)
    saludoBienvenida.remove("at: saludoBienvenida.index(before: saludoBienvenida.endIndex)")

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Dias", at: saludoBienvenida.endIndex)
    saludoBienvenida.remove("at: saludoBienvenida.index(before: saludoBienvenida.endIndex)")

let saludo = "Hola MUndo"
let indice = saludo.firstIndex(of:",") ?? saludo.endIndex
let comienzo = saludo[..<indice]
//Imprime "comienzo" es igual "Hola"
let nuevaCadena = String(comienzo)


let saludo = "Hola Mundo"
let indice = saludo.firstIndex(of:",") ?? saludo.index
let comienzo = [..<indice]
//Imprime commienzo es igual "Hola"
let nuevaCadena = String(comienzo)

let saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Dias", at: saludoBienvenida.endIndex)
    saludoBIenveida.remove("saludoBienvenida.index(before: saludoBienveida.endIndex)")

let saludo = "Hola Mundo"
let indice = saludo.firstIndex(of:",") ?? saludo.index
let comienzo = [..<indice]
//Imprime comienzo es igual "Hola"
let nuevaCadena = String(comienzo)


let frase = "Tu y yo nos parecemos"
let mismafrase = "Tu y yo nos parecemos"
    if frase == mismafrase {
        print("Estas dos frases son las mismas")
    }

let nombre = "Pedro Omar"
let mismoNombre = "Pedro Omar"
    if nombre == mismoNombre {
        print("Los dos nombres son iguales")
    }

let nombre = "Pedro"
let otroNombre = "Omar"
    if nombre != otroNombre {
        print("Los nombres son diferentes")
    }

let nombre = "Juan"
let otroNombre = "Pedro"
    if nombre != otroNombre {
        print("Los nombres son diferentes")
    }

let nombres = [
    "Cristiano Ronaldo",
    "Lionel Messi",
    "Andres Iniesta",
    "Xavi Hernandez",
    "Sergio Ramos"
]

var nombres = 0
    for nombres in jugadores {
        if nombres.hasPrefix("Cristiano Ronaldo") {
            jugadores += 1
        }
    }

    print("Hay \(nombres) jugadores ")
    //Imprime "Hay un jugador"

var conteoDeJugadores = 0
var conteoDeNombres = 0
    for nombres in jugadores {
        if nombres.hasSuffix("Cristiano Ronaldo") {
            conteoNombres += 1
        } else  if nombre.hasSuffix("Lionel Messi") {
            conteoJugadores += 1
        }
    }
print("\(conteoDeJugadores), \(conteoDeNombres) hay ahora mismo")
//Imrprime "cantidad de jugadores, cantidad de nombres"

let nombres = [
    "Jugador 1 Cristiano Ronaldo",
    "Jugador 2 Lionel Messi",
    "Jugador 3 Sergio Ramos",
    "Jugador 4 Xavi Hernanadez",
    "Jugador 5 Xabi Alonso"
]

var nombres = 0
    for nombres in jugador {
        nombres.hasSuffix("Cristiano Ronaldo") {
            jugadores += 1
        }
    }

var conteoJugadores = 0
var conteoNombres = 0
    for nombres in jugadores {
        if nombres.hasSuffix("Cristiano Ronaldo") {
            conteoJugadores += 1
        } else if nombres.hasSuffix("Lionel Messi") {
            conteoNombres += 1
        } 
    }

print("\(conteoJuegadores), \(conteoNombres) hay ahora mismo")
//Imprime "cantidad de jugadores", "cantidad de nombres"

let nombres = [
    "Jugador 1 Cristiano Ronaldo",
    "Jugadro 2 Lionel Messi",
    "Jugador 3 Sergio Ramos",
    "JUgador 4 Xabi Alonso",
    "Jugador 5 Xavi Hernandez"
]
var nombres = 0
    for nombres in judador {
        nombres.hasSuffix("Cristiano Ronaldo") {
            conteoJugador += 1
        }
    }
var conteoJugador = 0
var conteoNombres = 0
    for nombres in jugador {
        if nombres.hasSuffix("Cristiano Ronaldo") {
            conteoNombres += 1
        } else if nombres.hasSuffix("Lionel Messi") {
            conteoJugador += 1
        }
    }
print("\(conteoNombres), \(conteoJugador) hay ahora mismo")
//Imprime "conteoNombres", "conteoJugador"

let cadena = "Pez!!"
    for UnidadDeCodigo in cadenaPez {
        print("\(UnidadDeCodigo) ", terminator: "")
    }

var nombres: [String] = ("Pedro","Juan","Julio")
var numeros: [Int] = 1,2,3,4,5

var numero = 100
var numero: Double = 100

let nombres = """
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    """
var nombre: [Character] = ("¡","n","o","m","b","r","e","!")
var nombre = "Character"
    for character in ¡nombre! {
        //¡
        //n
        //o
        //m
        //b
        //r
        //e
        //!
    }

let signoDolar = "\{u34}"
let corazonRojo = "\{2665}"
let corazonBrillante = "\u{if496}"

var nombre = "Pedro"
var otroNombre = "Omar"
    if nombre != otroNombre {
        print("Los nombres son diferentes")
    }

var nombre = "Pedro"
var otroNombre = "Pedro"
    if nombre == otroNombre {
        print("Los nombres son iguales")
    }

var nombre = [
    "Jugador 1 Cristiano Ronaldo",
    "Jugador 2 Lionel Messi",
    "Jugador 3 Sergio Ramos",
    "Jugador 4 Xabi Alonso",
    "Jugador 5 Xavi Hernandez"
]
var nombre = 0
    for nombre in jugador {
        nombre.hasSuffix("Cristiano Ronaldo")
    }
var conteoDeNombre = 0
var conteoJugador = 0
    for nombre in jugador {
        if nombre.hasSuffix("Cristiano Ronaldo") {
            nombre += 1
        } else if nombre.hasSuffix("Lionel Messi") {
            nombre += 1
        }
    }
print("conteoNombre", "ConteoJugador")

let saludoBienvenida = "Buenos"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Dias", at: saludoBienvenida.endIndex)
    saludoBienvenida.remove("saludoBienvenida.index(before: saludoBienvenida.endIndex)")

var nombres: [String] = ("Pedro","Juan","Oscar")
    nombres.count() {
        print(nombres)
    }

let nombres: [String] = ("Pedro","Juan","Oscar")
    nombres.count() {
        print(nombres)
    }

let numeros: [Int] = (1,2,3,4)
    numeros.count() {
        print(numeros)
    }

var somInts: [Int] = []
    print(somInts es de tipo [Int] con \(someInts.count) items.")
    someInts.append(5)

var threeDoubles = Array(Repeating: 0.0, count: 3)
//Imprime three es de tipo Double, y es igual a (0.0, 0.0, 0.0)

var threeDoubles = Array(Repeating: 0.0, count: 3)
//Imprime three es de tipo Double, y es igual a (0.0, 0.0, 0.0)

var anotherthreeDoubles = Array(Repeating: 2.5, count: 3)
//Imprime anotherthreeDoubles es de tipo Doubles, y es igual (2.5, 2.5, 2.5)

var sixDoubles = anotherthreeDoubles + threeDoubles
//Imprime sixDoubles se infiere como [Double], y es igual a (0.0, 0.0, 0.0, 2.5, 2.5 2.5)

var sixDoubles = anotherthreeDoubles = threeDoubles
//Imprime sixDoubles se infiere como [Double], y es igual a (0.0, 0.0, 0.0, 2.5, 2.5, 2.5)

var nombres: [String] = ("Pedro","Juan","Carlos")
var numeros: [Int] = 1,2,3,4,5

let nombres: [String] = ("Pedro","Juan","Carlos")
let numeros: [Int] = 1,2,3,4,5

let nombres.count = String()

var cadena = """
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    Esta es una cadena de una sola linea
        Esta es una cadena de una sola linea
    """

let nombre: [Character] = ("¡","n","o","m","b","r","e","!")

var nombre = (Chracter)
    for character in ¡nombre! {
        //¡
        //n
        //o
        //m
        //b
        //r
        //e
        //!
    }

var saludoBienvenida = "Buenas"
    saludoBienvenida.insert("!", at: saludoBienvenida.endIndex)
    saludoBienvenida.insert(contentOf: "Dias", after: saludoBienvenida.endIndex)
    saludoBienvenida.remove("saludoBienvenida.index(before: saludoBienvenida.endIndex)")

var nombres = [
    "Jugador 1 Cristiano Ronaldo",
    "Jugador 2 Lionel Messi",
    "Jugador 3 Sergio Ramos",
    "Jugador 4 Xabi Alonso",
    "Jugador 5 Luka Modric"
]

var conteoNombre = 0
    for nombre in jugador {
        nombre.hasSuffix("Cristiano Ronaldo") {
            print(nombre)
        }
    }

var conteoNombre = 0
var conteoJugador = 0
    for nombre in jugador {
        if nombre.hasSuffix("Cristiano") {
            nombre += 1
        } else if nombre.hasSuffix("Lionel") {
            nombre += 1
        }
    }
print("conteoNombre", "conteoJugador")

var nombre = [String]()
    nombre.append("Pedro") {
        print(nombre)
    }

var numeros = Array(Repeating: 0.0, cons: 3)
//Imrprime numeros es de tipo doble y es igual a (0.0, 0.0, 0.0)

var AnotherThreeDoubles = Array(Repeating: 2.5, cons: 3)
//Imrprime AnotherthreeDoubles es de tipo Double y es igual a (2.5, 2.5, 2.5)

var datos = AnotherthreeDoubles + numeros
//Imprime datos se infiere como Double y es igual a (0.0, 0.0, 0.0, 2.5, 2.5, 2.5)

var numeros = [Int] = []
print("numeros es de tipo Entero con \(numeros.count) items")

var nombre = [String] = []
print("nombres es de tipo Cadena con \(nombres.count) items")

var nombre = [String] = []
    nombre.append = [Pedro] 
        print[nombre]
    

var numero = [Int] = []
    numero.append = [3] 
        print[numero]

var nombre = [String] = ["Pedro","Juan","Carlos"]
let nombre = [String] = ["Pedro","Juan","Carlos"]

var nombre = [String] = ["Pedro","Juan","Carlos"]
    print("La cantidad de nombre es \(nombre.count)")
    //Imrprime "Nombre" tiene 3 items

var nombre = [String] = ["Pedro","Juan","Carlos"]
    if nombre.isEmpty {
        print("La lista de nombre esta vacia")
    } else {
        print("La lista de nombre esta llena")
    }
print("La lista de nombre esta completamente vacia")

var nombre = [String] = ["Pedro","Juan","Carlos"]
    if nombre.isEmpty {
        print("La lista de nombre esta vacia")
    } else {
        print("La lista de nombre esta completamente llena")
    }
print("La lista de nombre esta vacia")

nombre.append["Jose"]
    print("Nombre")

nombre += ["Augusto","Omar","Alex"]
    print("Nombre")

var firstItems = nombre[0]

var nombre = [String] = ["Pedro","Juan","Carlos"]
    nombre.append["Jose"]
    if nombre.isEmpty {
        print("La lista de nombre esta vacia")
    } else {
        print("La lista de nombre esta llena")
    }
    print("La lista de nombre esta vacia")

    nombre += ["Augusto","Omar","Alex"]
        print(nombre)

var firstItems = nombre[2]

nombre[2] = ["Augusto"]
    print(nombre)
    // El segundo nombre de la cadena no es "Carlos" es "Augusto"

    nombre[nombre.count] = "Salt"
        print(nombre)

nombre.insert["Augusto"] = 3
    print(nombre)

nombre[4...6] = ("Alex","Omar")
nombre.insert["Alexis", at: 4]

var nombre = [String] = ("Pedro","Juan","Julio","Oscar","Jose")
    nombre.append["Alexis"]
    nombre.remove["Pedro"]
    nombre += ["Nicandro","Salvador","Juvenal"]
   
   if nombre.isEmpty {
        print("La lista nombre esta vacia")
    } else {
        print("La lista nombre esta llena")
    }
print("La lista nombre esta vacia")
    nombre.insert = [3] = ["Alexis"]
    nombre[4...6] = ["Alcalde","Luis"]
    nombre.insert["Alejandro", at: 5]
    nombre.removeLast()
        print(nombre)

    var nombres = [String] = ("Juanjo","Tony","Marta","Jose")
        for item in nombres {
            print(item)
        }
        //Manzanas
        //Peras
        //Platanos
        //Mamey
        //Mangos

        for index in enumerated() {
            print("Item \(index + 1): \(value)")
        }
        //Item 1: Manzanas
        //Item 2: Peras
        //Item 3: Guayabas
        //Item 4: Mangos
        //Iten 5: Platanos

var nombres = [String] = ("Pedro","Juan","Julio","Jose")
    nombre.append("Alex")
    nombre.insert["Luis", at: 5]
    nombre.removeLast()

var threeDoubles = Array[Repeating: 0, cons: 3]
    print("threeDoubles es de tipo de Double y es igual a 0.0, 0.0, 0.0")

var threeDoubles = Array[Repeating: 2.5, cons: 3]
    print("threeDoubles es de tipo Double y es igual a 2.5, 2.5, 2.5")

var SixDoubles = threeDoubles + threeDoubles
    print("SixDouble es de tipo Double y es igual a 0.0, 0.0, 0.0, 2.5, 2.5, 2.5")

var nombres = ("Pedro","Juan","Oscar")
    nombres.append("Alex")
    nombres.remove(2)
    nombre.removeLast()
    nombres.insert["Luis", at: 5]


    nombre.isEmpty()
        if nombre.isEmpty {
        print("La lista de nombre esta vacia")
        } else {
            print("La lista de nombre esta llena")
        }

var nombre = [String] = ("Pedro","Juan","Oscar")
    for item in nombre {
        //Manzanas
        //Mangos
        //Mamey
        //Aguacate
        //Anon
    }

    for index in enumerated() {
        print("Item  \(index + 1):  \(value)")
    }


var numeros = [Int] = []
    print("Numeros es de tipo Enteros y es igual a  \(numeros.count) items")

var nombre = [String] = ("Pedro","Juan","Oscar")
    nombre.insert("Alex", at: 3)
    nombre.remove("Oscar", at: 2)

    for item in enumerated() {
        print("item, values  \(nombre.enumerated)")
    }

    for (index, values) in nombre.enumerated() {
        print("Item  \(index + 1):  \(vale)")
    }
//Item 1: Bananas
//Item 2: Mangos
//Item 3: Aguacates
//Item 4: Manzanas
//Item 5: Mamey

var numero = 10
var numeros = 20
    if numero == numeros {
        print("Numero es igual a numeros")
    } else {
        print("Numero es diferente a numeros")
    }

var letters = Set<Character>()
    print("Set es de tipo Set<Character> con \(letters.count) items ")

    letters.insert("Pedro")
        print(letters)

    letters = []

var JugadoresFavoritos = Set<String> = ["Cristiano Ronaldo","Zinedine Zidane","Ronaldinho"]

var GenerosFavoritos: Set = ["Hip Hop", "Rock", "Pop"]

var GeneroFavorito: Set = ["Hip Hop", "Rock", "Pop"]
    print("Me gusta \(GeneroFavorito.count) generos de musica favoritas")
    if GeneroFavorito.isEmpty {
        print("Me gusta la musica pero no el Pop")
    } else {
        print("Tengo otras preferencias por la musica")
    }
//Imprime Tengo preferencias por otro tipo de musica

var generoFavorito: Set = ["Rock", "Hip Hop", "Pop"]
    if generoFavorito.isEmpty {
        print("No me gusta ese tipo de musica")
    } else {
        print("Tengo otro gusto por la musica")
    }
//Imprime Teengo preferencias por otro tipo de genero musical

let generoFavorito: Set = ["Rock", "Pop", "Hip Hop"]
    print("Mi tipo de genero es \(generoFavorito.count) cualquiera")
    generoFavorito.insert["Jazz"]

    if let removeGenero = generoFavorito.remove("Rock") {
        print("No me gusta el rock")
    } else {
        print("Nuca he escuchado rock")
    }

    if let removeGenero = generoFavorito.remove("Rock") {
        print("\(removeGenero)? No me gusta")
    } else {
        print("Nunca me ha gustado el rock")
    }

    //Imprime Rock? Ese genero nunca me ha gustado

    if let reoveGenero = generoFavorito.remove("Rock") {
        print("\(removeGenero)? No me gusta")
    } else {
        print("Nunca me ha gustado el rock")
    }
    //Imprime Rock? Ese genero nunca me ha gustado


let generoFavorito: Set = ["Rock", "Hip Hop", "Pop"]
    if let removeGenero = generoFavorito.remove["Rock"] {
        print("\(removeGenero)? No me gusta")
    } else {
        print("Nunca me ha gustado el rock")
    }
//Imprime Rock? Ese genero nunca me ha gustado

    if generoFavorito.contains["Rock"] {
        print("Es un buen genero")
    } else {
        print("Rock esta aqui")
    }
    //Imprime el Genero musical Rock aun esta vivo

var nombre: [String] = ("Pedro", "Juan", "Oscar")
    nombre.count()
    nombre.append("Alex")
    nombre.remove("Oscar")
    nombre.removeLast()

var nombre = String()
    if nombre.isEmpty {
        print("La cadena no esta vacia")
    } else {
        print("La cadena esta tolamente vacia")
    }
//Imprime La cadena "Nombre" esta vacia

var threeDouble = Array(Repeating: 0.0, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0")

var threeDoubles = Array(Repeating: 2.5, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 2.5, 2.5, 2.5")

var sixDoubles = threeDoubles + threeDoubles
    print("sixDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0, 2.5, 2.5, 2.5")

var generoFavorito = ("Rock", "Hip Hop", "Pop")
    generoFavorito.insert["Jazz"]
    generoFavorito.remove(1)
    generoFavorito.removeLast()

let generoFavorito = ("Rock", "Hip Hop", "Pop")
    if let removeGenero = generoFavorito.remove("Rock") {
        print("No me gusta el rock")
    } else {
        print("En la vida me ha gustado el rock")
    }
//Imprime Mi genero Favorito es el Pop

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    if let removeGenero = generoFavorito.remove("Rock") {
        print("\(removeGenero)? Ese genero no me gusta ")
    } else {
        print("Nunca me ha gustado el rock")
    }
//Imprime El rock nunca me ha gustado

let generroFavorito: Set = ("Rock", "Hip Hop", "Pop")
    generoFavorito.contains()

let generoFavorito: Set = ("Rock", "Hip Hop", "Pop")
    for generoFavorito in sorted() {
        print("\(generiFavorito)")
        //Clasico
        //Jazz
        //Regueton
    }

let generoFavorito: Set = ("Rock", "Hip Hop", "Pop")
    for generoFavorito in soported() {
        print("\(generoFavorito)")
        //Classical
        //Jazz
        //Regueton
    }

let DigitosImpares = [1,3,5,7,9]
let DigitosPares = [0,2,4,6,8]

var nombres: [String] = ("Pedro","Juan","Oscar")
var numeros: [Int] = (1,2,3,4,5)   
    nombres.append("Julio")
    nombres.remove("Julio")
    nombre.removeLast()
    nombre.reverse()

let threeDoubles = Array(Repeating: 0.0, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0")

let threeDoubles = Array(Repeating: 2.5, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 2.5, 2.5, 2.5")

let sixDoubles = threeDoubles + threeDoubles
    print("sixDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0, 2.5, 2.5, 2.5")

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    generoFavorito.insert("Jazz")
    generoFavorito.remove(2)
    generoFavorito.removeLast()

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    for removeGenero in generoFavorito.remove("Rock") {
        print("Nunca me ha gustado el Rock")
    } else {
        print("El Rock es buen genero pero no me gusta")
    }
//Imrprime Nunca me ha gustado el Rock como genero musical

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    for removeGenero = generoFavorito.remove("Rock") {
        print("\(removeGenero) Ese genero no me gusta")
    }

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    for generoFavorito in sorted() {
        print("\(generoFavorito)")
        //Jazz
        //Classical
        //Regueton
    }

    if generoFavorito.contains("Rock") {
        print("Ese genero no me gusta")
    } else {
        print("Interesante el Rock")
    }

    for genero in generoFavorito.sorted() {
        print("\(genero)")
        //Hip Hop
        //Rock
        //Pop
    }

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    generoFavorito.count() {
        print("Yo tengo \(generoFavorito.count) tre generos favoritos")
    }

    if generoFavorito.isEmpty() {
        print("No hay ningun genero que me guste")
    } else {
        print("Me gusta el Hip Hop como genero")
    }

    if let removedGenero = generoFavorito.remove("Rock") {
        print("\(removedGenero)? El Rock no me gusta")
    } else {
        print("El Rock me parece buen genero musical")
    }

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    generoFavorito.count() {
        print("Yo tengo \(generoFaovrito.count) tres generos favoritos")
    }

    if generoFavorito.Empty () {
        print("No hay ningun genero")
    } else {
        print("El rock parece un buen genero")
    }

    if let removedGenero = generoFavorito.remove("Rock") {
        print("\(removedGenero)? El Rock no me gusta")
    } else {
        print("El Rock no me parece mal genero")
    }
    //Imprime El Rock me parece buen genero

    if generoFavorito.contains("Rock") {
        print("No le encuentro")
    } else {
        print("EL Rock esta presente")
    }
    //Imprime El Rock es buen genero

let AnimalesDeCasa: Set = ["Perro","Gato"]
let AnimalesDeMonte: Set = ["Vaca","Chiva","Leon","Leopardo"]
let AnimalesDeCiudad: Set = ["Mosquitos","Moscas","Alcacranes"]
    AnimalesDeCasa.isSubset(of: AnimalesDeCasa)
    AnimalesDeCiudad.isSuperset(of: AnimalesDeCiudad)
    AnimalesDelMonte.isDisjoint(with: AnimalesDeCasa)


let animalesDeCasa: Set = ["Perro","Gato"]
let animalesDeMonte: Set = ["Leon","Leopardo","Vaca",Chiva]
let animalesDeCiudad: Set = ["Mosquitos","Moscas","Alacranes"]
    animalesDeCasa.isSubset(of: animalesDeCiudad)
    animalesDeMonte.isSuperset[of: animalesDeCiudad]
    animalesdeCiudad.isDisjoint[with: animalesDeCasa]

var nombresIntegrados: [Int: String] = [:]

var nombres: [String] = ("Pedro","Juan","Carlos")
    nombres.count()
    nombres.append("Jose")
    nombres.remove("Carlos")
    nombre.removeLast()

var generoFavorito: Set = ("Rock","Pop","Hip Hop")
    for removedGenero = generoFavorito.remove("Rock") {
        print("\(removedGenero) Ese genero no me gusta")
    }

let generoFavorito: Set = ("Rock", "Pop", "Rock")
    generoFavorito.contains("Rock") {
        print("\(generoFavorito) No lo encuentro")
    }
    for genero in generoFovorito.sorted() {
        print("\(genero)")
        //Classical
        //Jazz
        //Regueton
    }
    generoFavorito.insert("Jazz")

let animalesCasa: Set = ("Perro", "Gato")
let animalesDeMonte: Set = ("Vaca", "Leon", "Leopardo")
let animalesDeCiudad: Set = ("Raton", "Alacran", "Caballo")
    
    animalesDeCasa.isSubset("of: animalesDelMonte")
    animalesDelMonte.isSuperSet("of: animalesDeCiudad")
    animalesDeCiudad.isDisjoint("with: animalesDeCasa")

var nombresIntegrados: [String: Int] = [:]
var nombreIntegrado: [16] = "SixTeen"

var nombreIntegrado = [Int: 16, String: "Pedro"]
    print["nombreIntegrado"]

var nombres: [String] = ("Nombre": "Pedro", "Apellido": "Casabella", "Edad": 24)
var numeros: [Int] = (1,2,3,4,5)

var nombres: [String] = (Nombre: "Pedro", Apellido: "Casabella", Edad: 24)
        nombres.count() {
            print("Nombres tiene \(nombres.count) items")
        }
    if nombre.isEmpty() {
        print("La cadena no esta nombres esta vacia")
    } else {
        print("La cadena nombres esta vacia")
    }
    //Imprime La cadena nombres esta completamente vacia 

    nombres.append(Trabajo: "Profesor")
    nombre.remove(Nombre: "Pedro")
    nombre.removeLast()
    nombres["Juan"] = "Nuevo Nombre"

let nombre: [String] = "Pedro"
    if let oldValue = nombre.updateValue("Pedro", forKey: "Omar") {
        print("El verdadero nombre era \(oldValue)")
    }
//Imrpime El verdadero nombre era Pedro

let nombre: [String] = "Pedro Omar"
    if let oldValue = nombre.updateValue("Pedro", forValue: "Omar") {
        print("El verdadero nombre era \(oldValue)")
    }
//Imprime El verdadero nombre era Pedro Omar

let Aereopuerto: [String] = "Jose Marti"
    if let oldValue = nombre.updateValue("Jose", forKey: "Marti")
        print("El nombre del Aereopuerto es \(oldValue)")

let AereoPuerto: [String] = "Antonio Maceo"
    if let oldValue = Aereopuerto.updateValue("Jose", forKey: "Maceo") {
        print("El Aereopuerto se llama \(oldValue)")
    }
        
let Aereopuerto = "Jose Marti"
    if let nombreAereopuerto = Aereopuerto["HAB"] {
        print("El nombre del aereopuerto es \(nombreAereopuerto)")
    } else {
        print("El aereopuerto no tiene nombre")
    }
//Imprime El nombre del Aereopuerto es Internacional Jose Marti

let jugador = "Cristiano Ronaldo"
    if let nombreJugador = jugador["Cristiano Ronaldo"] {
        print("El nombre del jugador es \(nombreJugador)")
    } else {
        print("El jugador juega en el Real Madrid")
    }
//Imprime El verdadero nombre del jugador es "Cristiano Ronaldo"

let aereopuerto = "Jose Marti"
    if let oldValue = aereopuerto.updateValue("Jose", forKey: "Marti") {
        print("El nombre real del aereopuerto es \(oldValue)")
    }

let jugador = "Cristiano Ronaldo"
    if let nombreJugador = jugador["Cristiano Ronaldo"] {
        print("El nombre del jugador es \(nombreJugador)")
    } else {
        print("El jugador juega en el Real Madrid")
    }
//Imrpime El nombre real del jugador es "Cristiano Ronaldo"

var nombre: [String] = (Nombre: "Pedro", Apellido: "Casabella", Edad: 24)
    nombre.append("Juan")
    nombre.remove("Edad: 24")
    nombre.removeLast()

     nombre.isEmpty() {
        if nombre.isEmpty() {
        print("Si existe nombre")
    } else {
        print("La cadena esta completamente vacia")
    }
}
//Imprime La cadena nombre esta completamente vacia

let aereopuerto = "Jose Marti"
    if let oldValue = aereopuerto.updateValue("Jose", forKey: "Marti") {
        print("El verdadero nombre de aereopuerto es \(oldValue)")
    }
//Imprime El vardadero nombre de aereopuerto es "Jose Marti"

let jugador = "Cristiano Ronaldo"
    if let nombreJugador = jugador["Cristino Ronaldo"] {
        print("El nombre del jugador es \(nombreJugador)")
    } else {
        print("El verdadero nombre del jugador es Cristiano Romaldo")
    }
//Imprime El verdadero del jugador es "Cristiano Ronaldo"

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
    if let removedGenero = generoFavorito.remove("Rock") {
        print("\(removedGenero)? El rock no es buen genero")
    } else {
        print("El rock es escuchado en muchos paises")
    }
//Imprime El "Rock" es buen genero musical 

let generoFavorito: Set = ("Rock", "Hip Hop", "Pop")
    generoFavorito.insert("Jazz")
        for genero in generoFavorito.sorted() {
            print("\(generoFavorito)")
            //Cassical
            //Jazz
            //Bolero
        }

let generoFavorito: Set = ("Rock", "Pop", "Hip Hop")
        generoFavorito.contains() {
            print("\(generoFavorito) No lo encuentro")
        }
    //Imrprime No se encuentra un genero Favorito

let animalesDeCasa = ["Perro","Gato"]
let animalesDeMonte = ["Vaca","Cerdo","Chivo"]
let animalesDeCiudad = ["Caballo","Cotorra","Alacran"]
    animalesdeCasa.isSubset["animalesDelMonte"]
    animalesDeMonte.isSuperset["animalesDeCiudad"]
    animalesDeCiudad.isDisjoint["animalesDeCasa"]

let aereopuerto = "Jose Marti"
    if let nombreDeAereopuerto = aereopuerto["HAB"] {
        print("El nombre del Aereopuerto es \(nombreDeAereopuerto)")
    } else {
        print("Aereopuerto no aparece en la cadena")
    }
//Imprime El nombre del Aereopuerto Internacional es Jose Marti

let aereopuerto = "Jose Marti"
    if let nombreAereopuerto = aereopuerto["HAB"] {
        print("El nombre del aereopuerto es \(nombreAereopuerto)")
    } else {
        print("El nombre del Aereopuerto no aparece en la lista")
    }
//Imprime El nombre del aereopuerto internacional es Jose Marti

var nombre: [String] = ("Pedro","Juan",Carlos)
    nombre.count()
    nombre.append("Alex")
    nombre.remove("Pedro")
    nombre.removeLast()
    nombre.isEmpty() {
        print("No esta vacia")
//Imprime La cadena esta completamnete vacia

let generoFavorito: Set = ("Rock","Pop","Hip Hop")
    if let removedGenero = generoFavorito.remove("Rock") {
        print("\(removedGenero)? No lo encuentro")
    } else {
        print("El Rock es buen genero musical")
    }
//Imprime El Rock es buen Genero Musical pero no me gusta
    generoFavorito.isEmpty() {
        print("La cadena no esta vacia")
    }
//Imprime La cadena esta completamente vacia 

let generoFavorito: Set = ("Rock","Hip Hop","Pop")
    for genero in generoFavorito.sorted() {
        print("\(generoFavorito)")
        //Jazz
        //Bolero
        //Classical
    }
    generoFavorito.insert("Jazz")

let animalesDeCasa = ["Perro","Gato"]
let animalesDeMonte = ["Vaca","Caballo","Cebra"]
let animalesDeCiudad = ["Alacran","Cerdo","Pollo"]
    animalesDeMonte.isSubset["of: animalesDeMonte"]
    animalesDeCiudad.isSuperset["of: animalesDeCiudad"]
    animalesDeCasa.isDisjoint["with: animalesDeMonte"]

let threeDoubles = Array(Repeating: 0.0, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0")

let threeDoubles = Array(Repeating: 2.5, cons: 3)
    print("threeDoubles es de tipo Double y es igual a 2.5, 2.5, 2.5")

let sixDoubles = threeDoubles + threeDoubles
    print("sixDoubles es de tipo Double y es igual a 0.0, 0.0, 0.0, 2.5, 2.5, 2.5")

let generoFavorito: Set = ("Rock","Pop","Hip Hop")
    print("Yo tengo \(generoFavorito.count) generos favoritos")
//Imprime Yo tengo tre generos favoritos

let generoFavorito: Set = ("Rock","Hip Hop",Pop)
    if generoFavorito.contains("Rock") {
        print("Me parece buen genero")
    } else {
        print("El no me gusta como genero musical")
    }
//Imrprime El Rock es buen genero musical

let generoFavorito: Set = ("Rock","Hip Hop","Pop")
    for genero in generoFavorito {
        print("\(genero)")
    }
//Imprime La juvetud les gusta mucho la musica

let aereopuerto = "Jose Marti"
    if let oldValue = aereopuerto.updateValue("Jose", forKey: "Marti") {
        print("El aereopuerto no se llama \(oldValue)")
    } else {
        print("El aereopuerto se llama \(oldValue)")
    }
//Imprime El aereopuerto se llama Internacional Jose Marti

let nombreAereopuerto = aereopuerto["Hab"]
    if let nombreAereopuerto = aereopuerto["Hab"] {
        print("El aereopuerto se llama \(nombreAereopuerto)")
    } else {
        print("El nombre del aereopuerto no aparece")
    }
//Imrprime El nombre del Aereopuerto es Internacional Jose Marti

aereopuerto["Hab"] = "Aereopuerto Internacional"
aereopuerto["Hab"] = nil

if let removedValue = aereopuerto.removeValue["Hab"] {
    print("El nombre del aereopuerto eliminado es \(removedValue)")
} else {
    print("El aereopuerto no aparece con el nombre de la Habana")
}
//Imprime El nombre del Aereopuerto fue eliminado 

if let removedValue = aereopuerto.removeValue["Hab"] {
    print("El aereopuerto no aparece con el nomnbre de \(removedValue)")
} else {
    print("El aereopuerto aparece con el nombre de la Habana")
}//Imprime El nombre del Aereopuerto fue correctamente eliminado

if let removedValue = aereopuerto.removeValue["Hab"] {
    print("El aereopuerto no aparece el nombre de \(removedValue)")
} else {
    print("El aereopuerto aparece con el nombre de Internacional Jose Marti")
}
//Imprime El nombre del aereopuerto fue correctamente eliminado

for (nombreAereopuerto, codeAereopuerto) in aereopuerto {
    print("\(condeAereopuerto): \(nombreAereopuerto)")
}

for (nombreAereopuerto, codeAereopuerto) in aereopuerto {
    print("\(codeAereopuerto): \(nombreAereopuerto)")
}

for codeAereopuerto in aereopuerto.keys {
    print("codeAereopuerto: \(codeAereopuerto)")
}

for nombreAereopuerto in aereopuerto.values {
    print("\(nombreAereopuerto): \(nombreAereopuerto)")
}
//"Hab" Aereopueerto Jose Marti
//"Mtz" Aereopuerto Juan Gualberto Gomez

for nombreAerepuerto in aereopuerto.value {
    print("\(nombreAereopuerto): \(nombreAereopuerto)")
}
//Imprime "Hab" Aereopuerto Jose Marti
//Imprime "Mtz" Aereopuerto Juan Gualberto Gomez

let aereopuerto = [String](aereopuerto.keys)
//Imprime aereopuerto es "Hab" "Mtz"

let aereopuerto = [String](aereopuerto.values)
//Imprime Los nombres de los aereopuertos son (Jose Marti, Juan Gualberto Gomez)

let nombreAereopuerto = [String](nombreAereopuerto.keys)
    for nombreAereopuerto in nombreAereopuerto.keys {
        print("\(nombreAereopuerto): \(nombreAereopuerto)")
    }
//Imprime Los nombres de los aereopuertos son Jose Marti y Juan Gualberto Gomez

let codeAereopuerto = [String](codeAereopuerto.values)
    for codeAereopuerto in codeAereopuerto.values {
        print("\(codeAereopuerto): \("codeAereopuerto")")
    }
//Imprime los codigos de los aereopuertos son Hab y Mtz

var nombres: [String] = ("Pedro","Juan","Carlos")
    nombres.count() {
        print("nombres")
    }
    nombres.isEmty() {
        print("La cadena no esta vacia")
    }
    //Imprime La cadena esta completamente vacia 
    nombres.append("Alex")
    nombres.remove("Juan")
    nombres.removeLast()

let threeDoubles = Array(Repeating: 0.0, cons: 3)
    //threeDoubles es de tipo Double y es igual a [0.0, 0.0, 0.0]

let threeDoubles = Array(Repeating: 2.5, cons: 3)
    //Imprime threeDoubles es de tipo Double y es igual a [2.5, 2.5, 2.5]

let sixDoubles = threeDoubles + threeDoubles
    //Imprime sixDoubles es de tipo Double y es igual a [0.0, 0.0, 0.0, 2.5, 2.5, 2.5]

let generoFavorito: Set = ("Rock","Hip Hop","Pop")
    for genero in generoFavorito {
        print("\(genero)")
    }
    if let generoFavorito.contains("Rock") {
        print("no lo encuentro")
    } else {
        print("Es buen genero pero nome gusta")
    }
//Imrpime El Rock es un genero que tiene mucha fanaticada

    generoFavorito.insert["Jazz"]

let generoFavorito: Set = ("Rock","Pop","Hip Hop")
    if generoFavorito.sorted() {
        print("\(generoFavorito)")
        //Classical
        //Bolero
        //Jazz
    }

let generoFavorito: Set = ("Rock","Pop","Hip Hop")
    if let generoFavorito.remove("Rock") {
        print("El Rock no gusta")
    } else {
        print("El rock es buen genero)
    }
//Imprime El rock es buen genero y tiene bastante fanaticada

let animalesDeCasa = ["Perro","Gato"]
let animalesDeCiudad = ["Alacran","Cotorra","Paloma"]
let animalesDeMonte = ["Vaca","Cerdo","Leon"]
    animalesDeCasa.isSubset(of: animalesDeCiudad)
    animalesDeCiudad.isSuperset(of: animalesDeMonte)
    animalesDeMonte.isDisjoint(with: animalesdeCasa)

let aereopuerto = "Jose Marti"
    if let oldValue = aereopuerto.updateValue("Jose", forkey: "Marti") {
        print("El nombre del aereopuerto es \(oldValue)")
    } else {
        print("El Aereopuerto no tiene nombre")
    }
//Imprime El nombre del Aereopuerto es Internacional Jose Marti

let nombreAereopuerto = [String](nombreAereopuerto.keys)
    for nombreAereopuerto in nombreAereopuerto.keys {
        print("nombreAereopuerto: \(nombreAereopuerto)")
    }

let codeAereopuerto = [String](codeAereopuerto.values)
    for codeAereopuerto in codeAereopuerto.values {
        print("codeAereopuerto: \(codeAereopuerto)")
    }

let aereopuerto["Hab"] = "Aereopuerto Internacional"
    if let removeValue = aereopuerto.remove["Hab"] {
        print("El nombre del Aereopuerto es \(removedValue)")
    } else {
        print("El nombre del Aereopuerto no aparece")
    }
//Imprime el nombre del aereopuerto fue eliminado 

let aereopuerto["Hab"] = "Aereopuerto Internacional"
    if let removedValue = aereopuerto.remove["Hab"] {
        print("El nombre del aereopuerto es \(removedValue))
    } else {
        print("El nombre del aereopuerto fue eliminado")
    }
//Imprime El nombre del aereopuerto fue eliminado

let aereopuerto["Hab"] = "Aereopuerto Internacional"
    if let removedValue = aereopuerto.remove["Hab"] {
        print("El nombre del aereopuerto es \(removedValue)")
    } else {
        print("El nombre del aereopuerto fue eliminado")
    }
//Imprime Eo nombre del aereopuerto fue eliminado correctamente

let nombres = ("Pedro","Juan","Oscar")
    for nombre in nombres {
        print("Hola, \(nombres)!")
    }
    //Imprime Hola, "Pedro"
    //Imprime Hola, "Juan"
    //Imprime Hola, "Oscar"

let numeroAnimales = ("Caballos": 4, "Perros": 5, "Gatos": 2)
    for (nombreAnimal, Cantidad) in numeroAnimales {
        print("\(Cantidad) son \(numeroAnimales)")
    }
//Caballos son 4
//Perros son 5
//Gatos son 2

let numeroAnimales = ("Perros": 2, "Gatos": 5, "Cotorras": 4)
    for (nombreAnimal, CantidadAnimal) in nombreAnimales {
        print("\(CantidadAnimal) son \(numeroAnimales)")
    }
//"Perros" son 2
//"Gatos" son 5
//"Cotorras" son 4

for index in 1...5 {
    print("\(index) tiempo 5 es \(index * 5)")
}
//1 tiempo 5 es 5
//2 tiempo 5 es 10
//3 tiempo 5 es 15
//4 tiempo 5 es 20
//5 tiempo 5 es 25

let cantidadAnimales = ("Perro": 4, "Gatos": 3, "Caballos": 6)
    for (nombreAnimal, cantidadAnimal) in cantidadAnimales {
        print("\(cantidadAnimal) son \(nombreAnimal)")
    }
    //"Perros" son 4
    //"Gatos" son 3
    //"Caballos" son 6

    for index in 1...3 {
        print("\(index) tiempo 3 es \(index * 5)")
    }
    //1 tiempo 3 son 3
    //2 tiempo 3 son 6
    //3 tiempo 3 son 9

let nombres = ("Pedro","Juan","Oscar")
    for nombre in nombres {
        print("\(nombres)")
    }

let cantidadAnimales = ["Gatos": 4, "Perros": 6, "Caballos": 10]
    for (nombreAnimal, cantidadAnimal) in cantidadAnimales {
        print("\(cantidadAnimal) son \(nombreAnimales)")
    }
    //"Gatos": 4
    //"Perros": 6
    //"Caballos": 10

    for index in 1...5 {
        print("\(index) tiempo 5 es \(index * 5)")
    }
    //1 tiempo 5 es 5
    //2 tiempo 5 es 10
    //3 tiempo 5 es 15
    //4 tiempo 5 es 20
    //5 tiempo 5 es 25

let cantidadEquipo = ("Real Madrid": 23, "Barcelona": 22, "Juventus": 13)
    for (equipo, cantidadJugador) in cantidadEquipo {
        print("\(cantidadEquipo) son \(cantidadJugador)")
    }
    //"Real Madrid": 23
    //"Barcelona": 22
    //"Juventus": 13

let nombres = ("Pedro","Juan","Oscar")
    for nombre in nombres {
        print("\(nombres)")
    }

let 1....5 = "index"
    for index in 1...5 {
        print("\(index) tiempo 5 es \(index * 5)")
    }
    //1 tiempo 5 son 5
    //2 tiempo 5 son 10
    //3 tiempo 5 son 15
    //4 tiempo 5 son 20
    //5 tiempo 5 son 25

    let base = 3
    let apagado = 10
    var respuesta = 1
        for _ in 1..power {
            respuesta *= base
        }
    print("\(base) al poder de \(apagado) es \(respuesta)")
    //3 base al poder de 10 es 1 es 59049

let base = 3 
let poder = 10
let respuesta = 1
    for _ in 1..power {
        respuesta *= base
    }
    print("\(base) al poder de \(poder) es \(respuesta)")
    //3 base del poder de 10 es 59049

let nombres = ("Pedro","Juan","Oscar")
    for nombre in nombres {
        print("\(nombres)")
    }
    //Nombre "Pedro"
    //Nombre "Juan"
    //Nombre "Oscar"

let nombreEquipo = ("Real Maadrid": 4, "Barcelona": 5, "Sevilla": 6)
    for (cantidadEquipo, cantidadJugador) in nombreEquipo {
        print("\(cantidadEquipo) son \(cantidadJugador)")
    }
    //"Real Madrid" son 4
    //"Barcelona" son 5
    //"Sevilla" son 6

    for index in 1...5 {
        print("\(index) tiempo 5 son \(index * 5)")
    }
    //1 tiempo 5 son 5
    //2 tiempo 5 son 10
    //3 tiempo 5 son 15
    //4 tiempo 5 son 20
    //5 tiempo 5 son 25

let base = 3
let poder = 10
let respuesta = 4
    for _ in 1..power {
        print("\(base) el poder de \(poder) es \(respuesta)")
    }
    //Imprime 3 el poder de 10 es 59049

let base = 3
let poder = 10
let respuesta = 4
    for _ in 1..poder {
        respuesta *= base
    }
    print("\(base) el poder de \(poder) es \(respuesta)")
    //Imprime 3 el poder 10 es 59049

let base = 3
let poder = 10
let respuesta = 4
    for _ in 1..poder {
        respuesta *= base
    }
    print("\(base) el poder de \(poder) es \(respuesta)")

let nombres = ("Pedro","Juan","Carlos")
    for nombre in nombres {
        print("Hola, \(nombre)")
    }
    //Hola, "Pedro"
    //Hola, "Juan"
    //Hola, "Carlos"

let nombreJugador = ("RealMadrid": 4, "Barcelona": 5, "Sevilla": 6)
    for (nombreEquipo, cantidadJugador) in nombreJugador {
        print("\(nombreEquipo) son \(cantidadJugador)")
    }
    //"RealMadrid": 4
    //"Barcelona": 5
    //"Sevilla": 6

let minutos = 60
    for tickmark in 0..<minutos {
        //Representa la marca de verificacion cada (60 minutos)
    }

let minutos = 60
    for representa in 0..<minutos {
        //Representa la marca de verificacion cada (60 minutos)
    }

let minutoIntervalo = 5
    for tickmarck in stride(from: 0, to: minutos, by: minutoIntervalo) {
        //Representa la marca de verificacion cada 5 minutos(0, 5, 10, 15, ..., 45, 50, 55)
    }

let minutoIntervalo = 5
    for tickmarck in stride(form: 0, to: minutos, by: minutoIntervalo) {
        //Representa la marca de verificacion cada 5 minutos(0, 5, 10, 15, ..., 45, 50, 55)
    }

let minutos = 60
    for tickmark in 0..<minutos {
        //Representa la marca de verificacion cada (60 minutos)
    }

let minutos = 5
    for tickmark in stride(from: 0, to: minutos, by: minutoIntervalo) {
        //Representa la marca de verificacion cada 5 minutos(0, 5, 10, 15, ..., 45, 50, 55)
    }

let minutos = 60
    for tickmark in stride(from: 0, to: minutos, by: minutoIntervalo) {
        //Representa la marca de verificacion cada 5 minutos(0, 5, 10, 15, ..., 45, 50, 55)
    }

let minutos = 60
    for tickmark in 0..<minutos {
        //Representa la marca de verificacion cada 60 minutos
    }

let nombres = ("Pedro","Juan","Oscar")
    for nombre in nombres {
        print("Hola, \(nombres)")
    }
    //Hola, "Pedro"
    //Hola, "Juan"
    //Hola, "Carlos"

let nombreEquipos = ("Real Madrid": 4, "Barcelona": 5, "Sevilla": 6)
    for (nombreEquipo, cantidadJugadores) in nombreEquipos {
        //"Real Madrid": 4
        //"Barcelona": 5
        //"Sevilla": 6
    }
    print("\(nombreEquipo) son \(nombreJugadores)")

let horas = 12
let horaIntervalo = 3
    for tickmark in stride(from: 3, through: horas, by: horasIntervalo) {
        //Representa la marca de verificacion en 3 horas (3, 9, 12)
    }

let horas = 12 
let horasIntervalo = 3
    for tickmark in stride(from: 3, through: horas, by: horasIntervalo) {
        //Representa la marca de verificacion en 3 horas(3, 6, 9, 12)
    }

let nombres = ("Pedro","Juan","Carlos")
    for nombre in nombres {
        print("Hola, \(nombres)")
    }
    //Hola, "Pedro"
    //Hola, "Juan"
    //Hola, "Carlos"

let nombreEquipos = ("RealMadrid": 4, "Barcelona": 5, "Sevilla": 6)
    for (nombreEquipo, nombreJugador) in nombreEquipos {
        //"RealMadrid": 4
        //"Barcelona": 5
        //"Sevilla": 6
    }
    print("\(nombreEquipo) son \(nombreJugador)")

let minutos = 60
    for tickmark in stride(from: 0, to: minutos, by: minutos) {
        //Representa la marca de verificacion cada 5 minutos(0, 5, 10, 15, ..., 45, 50, 55)
    }

let base = 3
let poder = 10
let respuesta = 4
    for _ in 1..<poder {
        respuesta *= base
    }
    print("\(base) es el \(poder) de \(respuesta)")

let index = 5
    for index in 1...5 {
        print("\(index) tiempo 5 son \(index * 5)")
    }
    //1 tiempo son 5
    //2 tiempo son 10
    //3 tiempo son 15
    //4 tiempo son 20
    //5 tiempo son 25

let minutos = 60
    for tickmark in 1..<minutos {
        print("Representa la marca de verificacion cada 60 minutos")
    }

let horas = 12
let horasIntervalo = 3
    for tickmark in stride(from: 0, though: horas, by: horasIntervalo) {
        print("Representa la marca de verificacion en 3 horas(3, 6, 9, 12)")
    }
    
let horas = 12
let horasIntervalo = 3
    for tickmark in stride(from: 3, though: horas, by: horasIntervalo) {
        print("Representa la marca de verificacion en 3 horas(3, 6, 9, 12)")
    }

let minutos = 60
    for tickmark in 1..<minutos {
        print("Representala marca de verificacion cada 60 minutos")
    }

let index = 5
    for index in 1...5 {
        print("\(index) tiempo 5 son \(index * 5)")
    }
    //1 tiempo son 5
    //2 tiempo son 10
    //3 tiempo son 15
    //4 tiempo son 20
    //5 tiempo son 25

let horas = 12
let horasIntervalo = 3
    for tickmark in stride(from: 3, though: horas, by: horasIntervalo) {
        ("Representa la marca de verificacion en 3 horas(3, 6, 9, 12)")
    }

let minutos = 60
    for tickmark in 1..<minutos {
        print("Representa la marca de verificacion cada 60 minutos")
    }

let minutos = 60
    for tickmark in stride(from: 0, to: minutos, by: minutos) {
        print("Representa la marca de verificacion cada 60 minutos")
    }

while <#condition#> {
    <#statements#>
}

let finalSquare = 25
var board = [Int](repeating: 0, count: finalQuare + 1)

var square = 0
var square = 0
    while square < finalSquare {
    //roll to dice
        diceroll += 1
    
    if diceRoll == 7 {diceRoll = 1}
    //move by the rolled amount
        square += diceRoll

    if square < board.count {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo por una serpiente o una escalera
        square += board[square]
    }
}
print("Game Over!")

let nombres = ("Pedro","Juan","Carlos")
    for nombre in nombres {
        print("Hola, \(nombre)")
    }

let index = 5
    for index in 1...5 {
        print("\(index) tiempo son 5 \(index * 5)")
    }
    //1 tiempo son 5
    //2 tiempo son 10
    //3 tiempo son 15
    //4 tiempo son 20
    //5 tiempo son 25

let minutos = 60
    for tickmark in 1..<minutos {
        print("Representa la marca de verificacion en 60 minutos")
    }

let minutos = 60
    for tickmark in stride(from: 0, to: minutos, by: minutos)
        print("Representa la marca de verificacion en 60 minutos")
    }

let nombreEquipo = ("RealMadrid": 4, "Barcelona": 5, "Sevilla": 6)
    for (cantidadEquipo, cantidadJugador) in nombreEquipo {
        print("\(cantidadEquipo) son \(cantidadJugador)")
    }
    //"RealMadrid": 4
    //"Barcelona": 5
    //"Sevilla": 6

let base = 3
let poder = 10
let respuesta = 4
    for _ in 1..<poder {
        print("\(base) es el \(poder) de \(respuesta)")
    }

let horas = 12
let horasIntervalo = 3
    for tickmark in stride(from: 3, though: horas, by: horasIntervalos) {
        print("Repesenta la marca de verificacion en 3 horas (3, 6, 9, 12)")
    }

let horas = 12
let horasIntervalo = 5
    for tickmark in stride(from: 5, though: horas, by: horasIntervalo) {
        print("Representa la marca de verificacion en 5 horas(15, 20, 25, ..., 45, 50, 55)")
    }

let finalSquare = 0
var board = [Int](repeating: 0, count: finalSquare + 1)

let finalSquare = 25
var board = [Int](repeating: 0, count: finalSquare + 1)

var square = 0
var diceRoll = 0
    while diceRoll < finalSquare {
        //Tirar los dados
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
    //Tira los dados
        square += diceRoll
    if square < board.count {
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo por ua serpiente o una escalera
        square += board[square]
    }
}

let nombres = ("Pedro","Juan","Carlos")
    for nombre in nombres {
        print("Hola, \(nombres)")
    }
    //Hola, "Pedro"
    //Hola, "Juan"
    //Hola, "Carlos"

let base = 4
let poder = 10
let respuesta = 3
    for _ in 1..<poder {
        print("\(base) es el poder \(poder) de \(respuesta)")
    }

let index = 5
    for index in 1...5 {
        print("\(index) tiempo son 5 \(index * 5)")
    }
    //1 tiempo son 5
    //2 tiempo son 10
    //3 tiempo son 15
    //4 tiempo son 20
    //5 tiempo son 25

let minutos = 60
    for tickmark in 1..<minutos {
        print("Repesenta la marca de verificacion en 60 minutos")
    }

let minutos = 60
    for tickmark in stride(from: 0, to: minutos, by: minutos) {
        print("Representa la marca de verificacion en 60 minutos")
    }

let horas = 12
let horasIntervalo = 3
    for tickmark in stride(from: 3, though: horas, by: horasIntervalo) {
        print("Representa la marca de verificacion en \(horasIntevalo) horas(3, 6, 9, 12)")
    }

let horas = 12
let horasIntervalo = 5
    for ticmark in stride(from: 5, though: horas, by: horasIntervalo) {
        print("Representa la marca de verificacion en \(horasIntervalo) horas(5, 10, 15, 20, 25, ..., 45, 50, 55)")
    }

var square = 0
var diceRoll = 0 
    while square < finalSquare {
        //Tira los dados
        dideRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count {
        //Todavia estamos en el tablero, nos movemos hacia arriba y luego hacia abajo por una serpiente o una escalera
        square = board[square]
    }
}

var square = 0
var diceRoll = 0
    while square < finalSquare {
        //Tira los dados 
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo por una serpiente o una escalera
        square = board[square]
    }
}

var square = 0
var diceRoll = 0 
    while square < diceRoll {
        //Tira los dados
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
        square = board[square]
    }
}

var square = 0
var diceRoll = 0
    while square < diceRoll {
        //Tira los dados
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count {
        //Todavia estamos en el tablero, nos movemops hacia arriba y hacia abajo con una serpiente o una escalera
        square = board[square]
    }
}

let finalSquare = 25
var board = [Int](repeating: 0, count: finalSquare + 1)

var square = 0
var diceRoll = 0
    while square < diceRoll {
        //Tira los dados
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
        square = board[square]
    }
}

repeat {
    <#coments#>
} while <#condition#> 

let finalSquare = 0
var board = [Int](repeating: 0, count: finalSquare + 1)

repeat {
    //Nos movemos hacia arriba y hacia coon serpiente y una escalera 
    square += board[square]
    if diceRoll == 7 {diceRoll = 1}
    //Mueve los dados
    square += diceRoll 
} while square < finalSquare
print("Game Over!")

repeat {
    //Nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
    square += board[square]
    if diceRoll == 7 {diceRoll = 1}
    //Mueve los dados
    square += diceRoll 
} while square < finalSquare
print("Game Over")

repeat {
    //Nos movemos hacia arriba y hacia con una serpiente o una escalera
    square += board[square]
    if diceRoll == 7 {diceRoll = 1}
    //Muve los dados
    square += diceRoll
} while square < finalSquare
print("Game Over!")

repeat {
    //Nos movemos hacia arriba y hacia abajo con una serpiente o una escalera 
    square += board[square]
    if square == 7 {square = 1}
    //Mueve los dados
    square += diceRoll
} while square < finalSquare
print("Game Over!")

repeat {
    //Nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
    square += board[square]
    if square == 7 {square = 1}
    //Mueve los dados
    square += diceRoll
} while square < finalSquare
print("Game Over!")

var square = 0
var finalSquare = 0

while square < finalSquare {
        //Tira los dados
        diceRol += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count[square] {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con serpientes o un escalera
        square += finalSquare
    }
}
repeat {
        //Nos movemos hacia arriba y hacia abajo con serpientes o una escalera
        square += board
    if square == 7 {square = 1}
        //Mueve los dados
        square += diceRoll
} while square < finalSquare

print("Game Over!")

while square < finalSquare {
        //Tira los Dados
        diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        //Mueve los dados
        square += diceRoll
    if square < board.count[square] {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con serpientes y una escalera
        square += finalSquare
    }
}
repeat {
    //Nos movemos hacia arriba y hacia abajo con serpientes y una escalera
    square += diceRoll
if square == 7 {square = 1}
    //Mueve los dados
    square += diceRoll
} while square < finalSquare

print("Game Over")

var square = 0
var finalSquare = 0

while square < finalSquare {
    //Tira los dados
    diceRoll += 1
if diceRoll == 7 {diceRoll = 7}
    //Mueve los dados
    square += diceRoll
if square < board.count[square] {
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
    square += finalSquare
    }
}
repeat {
    //Tira los dados
    square += diceRoll
if square == 7 [square = 1]
    square += diceRoll
    //Nos movemos hacia arriba y hacia abajo con una serpiente una escalera
} while square < finalSquare
print("Game Over!")

var temperatureInFahrenheit = 30

if temperatureInFahrenheit <= 30 {
    print("Esta muy frio, Considera usar bufanda")
}
//Esta muy frio, considera usar bufanda

if temperatureInFahrenheit <= 30 {
    print("Esta muy frio, considera usar bufanda")
}
//Prints It's very cold, consider user scarf

temperatureInFahrenheit = 40

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else {
    print("It's not cold")
}

temperatureInFahrenheit = 40

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else {
    print("It's not cold")
}

var temperatureInFahrenheit = 90

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else if temperatureInFahrenheit >= 86 {
    print("It's really warm")
} else {
    print("It's not cold")
}
//Imprime It's not cold, It's really warm

var temperatureInfahrenheit = 90

if temperatureInFahrenheit <= 32 {
    prin("It's very cold, consider user scarf")
} else if temperatureInFahrenheit >= 86 {
    print("It's really warm")
} else {
    print("It's not cold")
}
//Imrprime It's really warm

var temperatureInFahrenheit = 90 

if temperatureInFahrenheit <=32 {
    print("It's cold, consider user scarf")
} else if temperatureInFahrenheit >= 86 {
    print("It's really warm")
} else {
    print("It's not cold")
}

temperatureInFahrenheit = 72

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else if temperatureInFahrenheit >= 86 {
    print("It's really warm")
}

var square = 0
var finalSquare = 0

while square < finalSquare {
    diceRoll += 1
    //Tira los dados
if diceRoll == 7 {diceRoll = 1}
    //Mueve los dados
    square += diceRoll
if square += board.count[square] {
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
    square += finalSquare
    }
}
repeat {
    //Tira los dados 
    square += diceRoll
if diceRoll == 7 {diceRoll = 1}
    square += diceRoll
    //Nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
} while square < finalSquare
print("Game Over!")

var temperatureInFahrenheit = 32

if temperature <= 32 {
    print("It's very cold, consider user scarf")
}
//Prints "It's very cold, consider user scarf"

var temperatureInFahrenheit = 72

if teperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else if temperatureInFahrenheit >= 86 {
    print("It's really warn, not consider user scarf")
} else {
    print("It's not cold")
}
//Prints "It's not cold, not use scarf"

let nombres: Character = "E"

switch nombres {
    case "A":
        print("Es A")
    case "B":
        print("Es B")
    case "C":
        print("Es C")
    case "D":
        print("Es D")
    default:
        print("No se ha encontrado ninguno")
}

let numeros = 10

switch numeros {
    case "1...4":
        print("Esta entre 1 y 4")
    case "5":
        print("Es 5")
    case "6..9":
        print("Esta entre 6 y 9")
    default:
        print("Ne encuantra aqui tampoco")
}

let letra: Character = "A"

switch letra {
    case "a": //Esta no es porque esta en minuscula
    case "A":
        print("Es A, porque esta en mayuscula")
    default:
        print("No es la letra A")
}
//Error de compilacion porque "a" esta en minuscula

var square = 0
var finalSquare = 0

while square < finalSquare {
    square += 1
    //Tira los dados
if diceRoll == 7 {diceRoll = 1}
    square += diceRoll
    //Mueve los dados
if square < board.count[square] {
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
    square += finalSquare
    }
}
repeat {
    //Tira los dados 
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    //Nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
} while square < finalSquare
print(""Game Over!)

var temperatureInFahrenheit = 32

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider using scarf")
}
//Prints "It's very cold, consider using scarf"

var temperatureInFarenheit = 72

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider using scarf")
} if else temperatureInFahrenheit >= 86 {
    print("It's really warn")
} else {
    print("It's not cold")
}

var nombres: Character = "A"

switch nombres {
    case "a": //Error de compilacion porque es con mayuscula
    case "A":
        print("Es A")
    default:
        print("No se encuentra en ninguna parte")
}

var nombres = "D"

switch nombres {
    case "A":
        print("Es A")
    case "B":
        print("Es B")
    case "C":
        print("Es C")
    case "D":
        print("Es D")
    default:
        print("No se encuentra en ninguna que aparece ahi")
}

var numero: [Int] = 10

switch numero {
    case "1":
        print("Es 1")
    case "2..5":
        print("Esta entre 2 y 5")
    case "6":
        print("Es 6")
    case "6..9":
        print("Esta entre 6 y 9")
    case "10":
        print("Es 10")
    default:
        print("No esta en ningumo de los casos")
}

let letra: Character = "A"

switch letra {
    case "a, A":
        print("Es la letra A")
    default:
        ("No es ninguna de ellas")
}
//Prints "Is the letter A"

var square = 0
var finalSquare = 0

while square < finalSquare {
    square += 1
    //Tira los dados
if diceRoll == 7 {diceRoll = 1}
    square += diceRoll
    //Mueve los dados
if square < board.count[square] {
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
    square += finalSquare
    }
}
repeat {
    //Tira los dados
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    //Nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
} while square += finalSquare
print("Game Over!")

var temperatureInFahrenheit = 32

if temperatureInFahrenheit <= 32
    print("It's very cold, consider using scarf")

var temperatureInFahrenheit = 72

if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else if temeperatureInFahrenheit >= 86 {
    print("It's really warm")
} else {
    print("It's not cold, not is necesary user scarf")
}
//Prints "It's very cold, consider user Scarf"

var letra: Character = "D"

switch letra {
    case "A":
        print("Es A")
    case "B":
        print("Es B")
    case "C":
        print("Es C")
    case "D":
        print("Es D")
    default:
        print("No se encuentra entre ninguna de ellas") 
}

var letra = "A"
    switch letra {
        case "a, A"://Eso es un error de compilacion porque es la letra "A"
            print("Es la letra A")
        default:
            print("No es ninguna de ella")
    } if letra = a {
        print("No es esa")
    } else {
        print("La letra es "A")
    }

var temperatureInCelsius = 25
let weatherConsejos: String
    if temperatureINCelsius <= 0 {
        weatherAdvice = "It's very cold, consider user scarf"
    } else if temperatureInCelsius >= 30 {
        weatherAvice = "It's really warm"
    } else {
        weatherAdvice = "It's not cold"
    }
print(weatherAdvice)

let weatherAdvice = if temperatureInCelsius <= 0 {
    "It's very cold, consider user scarf"
} else if temperatureInCelsius >= 30 {
    "It's really warm"
} else {
    "It's not cold"
}
print(weatherAdvice)

let freezeWarning: String? = if temperatureInCelsius <= 30 {
    "Esta por debajo del punto de congelacion, cuidado con el hielo"
} else {
    cero
}

var square = 0
var finalSquare = 0

while square < finalSquare {
    square += 1
    //Tira los dados
if diceRoll == 7 {diceRoll = 1}
    square += diceRoll
    //Mueve los dados
if square < board.count[square]
    //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
    square += finalSquare
}
repeat {
    //Tira los dados 
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    //Nos movemos hacia arriba y hacia abajo con una serpiente y una escalera
} while square < finalSquare
print("Game Over!")

var temperatureInFahrenheit = 32
    if temperatureInFahrenheit <= 32 {
        print("It's very cold, consider user scarf")
    }

var temperatureInFahrenheit = 72
    if temperatureInFahrenheit <= 32 {
        print("It's very cold, consider user scarf")
    } else if temperatureInFahrenheit >= 86 {
        print("It's really warm")
    } else {
        print("It's not very cold")
    }
print("It's very cold, consider user scarf")

let temperatureInFahrenheit: String? = if temperatureInFahrenheit <= 32 {
    print("It's very cold, consider user scarf")
} else {
    print("It's not cold ")
}

let letra = "F"
    switch letra {
        case "A..D":
            print("Esta entre A y D")
        case "E":
            print("Es E")
        case "F":
            print("Es F")
        default:
            print("NO esta entre ninguna de ellas")
    }

let temperatureInFahrenheit = 32
let weatherAdvice: String
    if temperatureInFarenheit <= 32 {
        weatherAdvice = "It's very cold, consider user scarf"
    } else if temperatureInFahrenheit >= 86 {
        weatherAdvice = "It's really warn"
    } else {
        weatherAdvice = "It's not cold"
    }

let temperatureInFahrenheit = 32
let weatherAdvice: String
    if temperatureInFahrenheit <= 32 {
        weatherAdvice = "It's very cold, consider user scarf"
    } else if temperatureInFahrenheit >= 86 {
        wheaterAdvice = "It's really warm"
    } else {
        weatherAdvice = "It's not cold"
    }
print(weather)

let temperatureInFahrenheit = 0
let weatherAdvice: Stirng

let weatherAdvice = if temperatureInFahrenheit <= 0 {
    "It's very cold, consider user Scarf"
} if else temperatureInFahrenheit >= 0 {
    "It's really warm"
} else {
    "It's not cold"
}

let freezeWarnig: String? = if temperatureInCelsius <= 0 {
    "It's below frezzing. Watch for ice"
} else {
    nil
}

let freezingWarnig: String? = let temperatureInCelsius <= 0 {
    "It's below freezing. Whatch for ice!"
} else {
    "nil"
}

let freezeWarning: String? = if temperatureInCelsius <= 0 {
    "It's bellow freezing. watch for ice"
} else {
    "nil as String?"
}

let freezing: String = if temperatureInCelsius <= 0 {
    "It's bellow freezing. Watch for Ice"
} else {
    nil as String
}

let freesing: String = if temperatureInCelsius > 100 {
    throw TemperatureError.boiling 
} else {
    "It's a reasonable temperature"
}

let freezing: String? = if temeperatureInCelsius <= 0 {
    "It's bellw freezing. Watch for ice"
} else {
    nil as String
}

let weatherAdvice = if temperatureInCelsius >= 100 {
    throw TemperatureError.bolling
} else {
    "It's reasonable temperature"
}

let weatherAdvice = if temperatureInCelsius >= 100 {
    throw TemperatureError.bolling
} else {
    "It's reasonable temperature"
}

let freezing = temperatureInCelsius <= 30 {
    "It's bellow freezing, Watch for ice"
} else {
    nil a String
}

let weatherAdvice = if temperatureInCelsius >= 100 {
    throw TemperatureError.bellow
} else {
    It's reasonable temperature
}

var square = 0
var finalsquare = o
    while square < finalSquare {
        square += 1
        //Tira los dados
    if diceRoll == 7 {diceRoll = 1}
        square += diceRoll
        //Mueve los dados
    if square < board[square] {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
        square += diceRoll
    }
}
repeat {
    //Tira los dados
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    //Nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
} while square < finalSquare

print("Game Over!")

let temperatureInFahrenheit = 32 
    if temperatureInFahrenheit <= 32 {
        print("It's very cold, consider user scarf")
    }

let temperatureInFahrenheit = 72 
    if temperatureInFahrenheit <= 32 {
        print("It's very cold, consider user scarf")
    } else if temperatureInFahrenheit >= 86 {
        print("It's really warn")
    } else {
        print("It's not cold, not necesary user scarf")
    }
print("It's very cold, consider user scarf")

let weatherAdvice = if temperatureInFhrenheit <= 32 {
    weatherAdvice = "It's very cold, consider user scarf"
} else if temperatureInFahrenheit >= 86 {
    weatherAdvice = "It's really warn"
} else {
    weatherAdvice = "It's not cold"
}
print("It's not cold")

var letra = "F"
    switch letra {
        case "A":
            print("Es A")
        case "B..E"   
            print("Esta entre B y E")
        case "F":
            print("Es F")
        default:
            print("No esta enre ninguna de ellas")
            
    }

var letra: String = "F"
    switch letra {
        case "A":
            print("Es A")
        case "B":
            print("Es B")
        case "C..F"
            print("Esta entre C y F")
        default:
            print("No esta entre ninguna de ellas")
    }
print(letra)

let freezing = if temperatureInCelsius <= 32 {
    "It's bellow freezing. Watch for ice"
} else {
    nil as String?
}

let weatherAdvice = if temperatureInCelsius <= 32 {
    throw TemperatureError.boiling
} else {
    "It's reasonable temperature"
}

let freezing = if temperatureInCelsius <= 32 {
    "It's bellw freezing. Watch for ice"
} else {
    nil as String?
}

let weatherAdvice = if temperatureInCelsius <= 100 {
    throw TemperatureError.boiling
} else {
    "It's reasonable temperature"
}

let freezing = if temperatureInCelsius <= 32 {
    "It's bellow freezing. Watch for ice"
} else {
    nil as String?
}

let weatherAdvice = if temperatureInCelsius <= 32 {
    throw TemperatureError.boilling
} else {
    "It's reasonable temperature"
}

let anotherCharacter: Character = "A"
    switch Character {
        case "a": //Esto es un error de ejecucion
            print("Es A")
        case "A":
            print("Es la letra A")
        default:
            print("No es ninguna de ellas") 
    }
print("AnotherCharacter")

let anotherCharacter: Character = "a"
    switch Character {
        case "a", "A":
            print("Es la letra a")
        default:
            print("No es ninguna de ellas")
}
// Es la letra "a"

let aproximateCount = 62
let countedThings = "moons orbiting Saturn"
let naturalCount: String
    switch aproximateCount {
        case 0:
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCount = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }
print("There are \(naturalCount) \(countedThings)")

let aproximateCount = 62
let countedThings = "moons orbiting Saturn"
let naturalCount: String
    switch aproximatedCount {
        case 0:
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCiunt = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }

let aproximateCount = 62
let countedThings = "moons orbitting saturn"
let notableCount: String
    switch aproximateCount {
        case 0:
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCount = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }
print("There are \(notableCount) \(countedThings)")

let aproximateCount = 62
let countedThings = "moons orbitting saturn"
let naturalCount: String
    switch aproximateCount {
        case 0:
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCount = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }

let somePoint = (1, 1)
    switch somePoint {
        case (0, 0):
            print("\(somepoint) is at origin")
        case (_, 0):
            print("\(somePoint) is on the x-axis")
        case (0, _):
            print("\(somePoint) is on the x-axis")
        case (-2...2, -2...2):
            print("\(somePoint)is inside the box")
        default:
            print("\(somePoint)is outside of the box")
    }
print("\(somePoint) is inside the box")

let aproximateCount = 62
let countedThings = ("moons orbiting saturn")
let naturalCount: String
    switch aproximatedCount {
        case "0":
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCount = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }

print("They are \(naturalCount) \(countedThings)")

let somePoint = (0,0)
    switch somePoint {
        case (0, 0):
            print("\(somePoint) is at the origin")
        case (_, 0):
            print("\(somePoint) is at on x-axis")
        case (0, _):
            print("\(somePoint) is on the y-axis")
        case (-2...2, -2...2):
            print("\(somePoint) is inside the box")
        default:
            print("(somePoint)  is outside of the box")
    }

let somePoint = "(0, 0)"
    switch somePoint {
        case (0, 0):
            print("\(somePoint) is at the origin")
    }

let aproximatedCount = 62
let countedThings = "moons orbiting saturn"
let naturalCount: String
    switch aproximatedCount {
        case 0:
            naturalCount = "no"
        case 1..<5:
            naturalCount = "a few"
        case 5..<12:
            naturalCount = "several"
        case 12..<100:
            naturalCount = "dozens of"
        case 100..<1000:
            naturalCount = "hundreds of"
        default:
            naturalCount = "many"
    }
print("They are \(naturalCount) \(countedThings)")

let somePoint = (1, 1)
    switch somePoint {
        case (0, 0):
            print("\(somePoint) is at the origin")
        case (_, 0):
            print("\(somePoint) is at the x-axis")
        case (0, _):
            print("\(somepoint is at the y-axis)")
        case (2...-2, -2...2):
            print("\(somePoint) is inside the box")
        default:
            print("\(somePoint) is outside of the box")
    }

let anotherPoint = (2, 0)
    switch anotherPoint {
        case (let x, 0):
            print("on the x-axis with an x value of \(x)")
        case (0, let y):
            print("on the y-axis a y value of \(y)")
        case (let x, y):
            print("somewhere else at \(x) \(y)")
    }

let yetAnotherPoint = (-1, 1)
    switch yetAnotherPoint {
        case let (x, y) where x == y:
            print("\(x), \(y) is on the line x == y")
        case let (x, y) where x == -y:
            print("\(x), \(y) is on the line x == -y")
        case let (x, y):
            print("(\(x), \(y)) is just some arbitrary point")
    }
// Prints "(1, -1) is on the line x == -y" 

let yetAnotherPoint = (-1, 1)
    switch yetAnotherPoint {
        case let (x, y) where x == y:
            print("\(x), \(y) is on the line x == y")
        case let (x, y) where x == -y:
            print("\(x), \(y) is on the line x == -y")
        case let (x, y):
            print("(\(x), \(y)) is just some arbitrary point")
    }
// Prints "(-1, 1) is on the line x == -y"

let yetAnotherPoint = (-1, 1)
    switch yetAnotherPoint {
        case let (x, y) where x == y:
            print("\(x), \(y) is on the line x == y")
        case let (x, y) where x == -y:
            print("\(x), \(y) is on the line x == -y")
        case let (x, y):
            print("\(x), \(y) is just some arbitrary point")
    }
//Prints "(1, -1) in on the line x == -y"

let stillAnotherPoint = (9, 0)
    switch stillAnotherPoint {
        case (let distance, 0), (0, let distance):
            print("On an axis, \(distance) from the origin")
        default:
            print("Not on a axis")
    }
//Prints "On a axis, 9 from the origin"

let stillAnotherPoint = (9, 0)
    switch stillAnother {
        case (let dstance, 0), (0, let distance):
            print("On an axis, \(distance) from the origin")
        default:
            print("Not on axis")
    }
//Prints "On an axis, 9 from the origin"

let stillAnotherPoint = (9, 0)
    switch stillAnotherPoint {
        case (let distance, 0), (0, let distance):
            print("On an axis, \(distance) from the origin")
        default:
            print("Not on axis")
    }

let temepratureInFahrenheit = 32
    if temperatureInFahrenheit <= 32 {
        print("It's user scarf")
    }

let temperatureInFahrenheit = 72
    if temperatureInFahrenheit <= 32 {
        print("It's very scarf")
    } else let temperatureInFahrenheit >= 86 {
        print("It's really warm")
    } else {
        print("It's not user scarf")
    }

let square = 0
var diceRoll = 0
    while square < diceRoll {
        square += 1
        //Tira los dados
    if diceRoll == 7 {diceRoll = 1}
        square += diceRoll
        //Mueve los dados
    if square < board.count[square] {
        //Todavia estamos en el tablero, nos movemos hacia arriba y hacia abajo con una serpiente o una escalera
        square += finalSquare
    }
}
repeat {
    //Subir o bajar por una serpiente o una escalera
    square += finalSquare[square]
    //Tira los dados
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    //Mueve los dados
    square += diceRoll
} while square < finalSquare
//Prints (Game Over!)

var temperatureInCelsius = 32
let weatherAdvice: String
    if temperatureInCelsius <= 32 {
        weatherAdvice = "It's very cold, consider user scarf"
    } else if temperatureInCelsius >= 32 {
        weatherAdvice = "It's really warm"
    } else {
        weatherAdvice = "It's not cold"
    }

let puzzleInput = "Las grandes mentes piensan igual"
var puzzleOutput = ""
let charactersToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character in puzzleInput {
        if characters.contains(character) {
            continue
        }
        puzzleOutput.append(character)
    }
print("puzzleOutPut")
//Prints "grmndsthnklk"

let puzzleInput = "Las grandes mentes piensan igual"
var puzzleOutPut = ""
let characterToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character in puzzleOut {
        if character.contains(character) {
            continue
        }
        puzzleOutPut.append(character)
    }
print("puzzleOutPut)

let puzzleInput = "Las grandes mentes piensan igual"
var puzzleOut = ""
let CharacterToRemove: [Character] = ["a", "e", "i", "o", "u", ""]
    for character in puzzleOut {
        if character.contains(character) {
            continue
        }
        puzzle.append(character)
    }
print(puzzleOut)

let puzzleInput = "las grandes mentes piensan igual"
var puzzleOut = ""
let CharacterToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character un puzzleOut {
        if character.contains(character) {
            continue
        }
        puzzle.append(character)
    }
print(puzzleOut)

let puzzleInPut = "Las grandes mentes piensan igual"
let puzzleOut = ""
let CharacterToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character in puzzleInPuut {
        if character.contains(character) {
            continue
        }
        puzzle.append(character)
    }
print(puzzleOut)

let puzzleInPut = "Las grandes mentes piensan igual"
let puzzleOut = ""
let CharacterToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character in puzzleOut {
        if character.contains(character) {
            continue
        }
        puzzle.append(character)
    }
print(puzzleOut)

let puzzleInput = "Las grandes mentes piensan igual"
let puzzleOut = ""
let CharacterToRemove: [Character] = ["a", "e", "i", "o", "u", " "]
    for character in puzzleOut {
        if character.contains(character) {
            continue
        }
        puzzle.append(character)
    }
print(puzzleOut)

let intergerToDescribe = 5
var description = "The number \(intergerToDescribe) is"
    switch intergerToDescribe {
        case 2, 3, 5, 7, 11, 13, 17, 19:
            description += "a prime number, and also"
            fallthrough
        default:
            description += "an interger"
    }
//Prints "The number 5 is a prime number, and also an interger"

let intergerToDescribe = 5
var description = "The numer \(intergerToDescribe) is"
    switch intergerToDescribe {
        case 2, 3, 5, 7, 11, 13, 17, 19:
            description += "a prime number, and also"
            falthrough
        default:
            description += "an interger"
    }
// Prints "The numbre 5 is a prime number, and also"

let intergerToDescribe = 5
var description = "The number \(intergerToDescribe)"
    switch intergerToDescribe {
        case 2, 3, 5, 7, 11, 13, 17, 19:
        description += "an interger"
        fallthrough
        default:
            description += "an interger"
    }
// Prints "The number 5 is a prime number, and also"

gameloop: square != finalSquare {
    diceRoll += 1
    if diceRoll == 7 {diceRoll = 1}
        switch square + diceRoll {
            case finalSquare:
                diceRoll nos movera a la casilla final, por lo que el juego ha terminado.
        Romper gameloop
            case if newSquare where newSquare < finalSquare:
                diceRoll nos movera mas alla de la casilla final
            continue gameloop
            Value predeterminado:
                Esto es un movimiento valido, asi que averigue su efecto 
        square += diceRoll
        square += diceRoll[square]
        }
}
print("!Se acabo el juego!")

gameLoop: square != finalSquare {
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    switch square + diceRoll {
        case finalSquare:
            diceRoll nos movera hasta la casilla final, por lo que le juego ha terminado.
        continue gameLoop
        default:
            Esto es un movimiento valido, asi que averigua su efecto
        square += diceRoll
        square += diceRoll[square]
    }
}
print("!Se acabo el juego!")

gameLoop: square != finalSquare {
    diceRroll += 1
if diceRoll == 7 {diceRoll = 1}
    switch square + diceRoll {
        case finalSquare:
            diceRoll nos movera hasta la casilla final, por lo que el juego ha terminado.
            break gameLoop
        case if newSquare where newSquare < finalSquare:
            diceRoll nos movera mas alla de la casilla final, asi que vuelve a rodar. 
            continue gameLoop
        default: 
            Esto es un movimeinto valido, asi que averigua su efecto.
        square += diceRoll
        square += diceRoll[square]
    }
}
print("!Game Omver!")

gameLoop: square != finalSquare {
    diceRoll += 1
if diceRoll == 7 {diceRoll = 1}
    switch square + diceRoll {
        case finalSquare:
            diceRoll nos movera hasta la coasilla final, por lo que el juego ha terminado.
            break gameloop
        case if newSquare where newSquare < finalSquare:
            diceRoll nos movera mas alla de la casilla final
            continue gameLoop
        default:
            Esto es una movimiento valido, asi que averigua su efecto.
        square += diceRoll
        square += diceRoll[square]
    }
}

func greet(person: [Strin: String]) {
    guard let name = person["name"] else {
        return
    }
    print("hola \(nombre)!")

    guard let location = person["Location"] else {
        print("Espero que el clima sea agradable cerca de ti")
            return
    }
        print("Espero que el clima sea agradable en \(ubicacion).")
}
saludar(persona: ["nombre": "Juan"])
Print "Hello Pedro!"
Prints "Espero que el clima sea agradable cerca de ti"
saludar(persona: ["nombre": "Juan", "Oscar", "Pedro"])
Print "Hello Pedro!"
Prints "Espero que pases una buen tiempo en Cupertino"

func greet(person: [String: String]) {
    guard let name = person["name] else {
        return
    }
    print("Hola \(nombre)!")

    guard let location = person["Location"] else {
        print("Espero que el clima sea agradable cerca de ti)
    }
    print("Espero que el clima sea agradable en \(Location).")
}
saludar(persona: ["nombre": "Pedro"])
print "Hello Pedro!"
Prints "Espero que el clima sea agradable para ti"

saludar(persona: ["nombre": "Juan", "Oscar", "Pedro"])
print "Hello Pedro!"
prints "Espero que pases un buen tiempo en Cupertino"

func greet(person: [String: String]) {
    guard let name = person["name"] else {
        return
    print("Hola, \(nombre)!")

    guard let location = person["Location"] else {
        print("Espero que el clima sea agradable para ti")
    }
    print("Espero que el clima sea agradable en \(Location)")
    }
}
saludar(persona: ["Nombre": "Pedro"])
print "Hello Pedro!"
prints "Espero que el clima sea agradable paras ti"

saludar(persona: ["nombre": "Pedro", "Juan", "Oscar"])
print "Hello Pedro!"
prints "Espero que el clima sea agradable en Cupertino"

Puntuacion Var = 1
    if puntuacion < 10 {
        defer {
            print(puntuacion)
        }
        puntuacion += 5
    }
Prints "5"

Puntuacion Var = 1
    if puntuacion < 10 {
        defer {
            print(puntuacion)
        }
        puntuacion += 5
    }
Prints "5"

Puntuacion Var = 3
    if puntucuaion < 100 {
        puntuacion += 100
            defer {
                puntuacion -= 100
            }
            Otro codigo que usa la puntuacion con su bono va aqui
        print(puntuacion)
    }

Puntuacion Var = 3
    if puntucacion < 100 {
        puntuacion += 100
            defer {
                puntuacion -= 100
            }
            Otro codigo que usa la puntuacion con su bono va aqui
                print(puintuacion)
    }

func greet(person: [String: String]) {
    guard let name = person["name"] else {
        return
    
    print("Hola \(nombre)!")
    }
    
    guard let name = person["Location"] else {
        return
    }
    print("Espero que el clima sea agradable en \(Location)")
}

saludar("person: ["name": "Pedro"]")
Print("Hola Pedro!")
Prints "Espero que el clima sea agradable cerca de ti"

saludar("Person: ["name": "Pedro", "Juan", "Carlos"]")
Print("Hola Juan!")
Prints "Espera que sea agradable el dia en Cupertino"

func greet(person: [String: String]) {
    guard let name = person["name"] else {
        return
    }

    print("Hola \(name)")
    
    guard let name = person["Location"] else {
        return
    }

    print("Espero que el clima sea agradable en \(Location)")
}
saludar("name": "Pedro")
Print("Hola Pedro!")
Prints "Espero que el clima sea agradable cerca de ti"

saludar("name": "Pedro", "Juan"), "Carlos"
Print("Hola Juan!")
Prints "Espero que sea agradable el dia en Cupertino"

Puntuacion Var = 1
    if puntuacion < 10 {
        defer {
            print(Puntuacion)
        }
        Puntuacion += 5
    }
Prints "6"

Puntuacion Var = 1
    if puntuacion < 10 {
        defer {
            print(puntuacion)
        }
        puntuacion += 5
    }
Prints "6"

Puntuacion Var = 3
if puntuacion < 100 {
  puntuacion += 100
    defer {
  puntuacion -= 100
    }
    Otro codigo que usa la puntuacion con su bono va aqui.
  print(puntuacion)
}
Prints "103"

puntuacion Var = 3
if puntuacion < 100 {
  puntuacion += 100
    defer {
  puntuacion -= 100
    }
  print(puntuacion)
}
Prints "103"

if puntuacion < 10 {
    defer {
        print(puntuacion)
    }
    defer {
        print("la partidura es:")
    }
    puntuacion += 5
}
print "La partidura es:"
prints "6"

var puntuacion = 3
if puntuacion < 10 {
        puntuacion += 100
    defer {
        puntuacion -= 100
    }
    print(puntuacion)
}

if puntuacion < 10 {
    defer {
        print(puntuacion)
    }
    defer {
        print(puntuacion)
    }
    puntuacion += 5
}
print "la puntuacion es:"
prints "6"

@available(macOS 10.12, *)
struct ColorPreference {
    var bestColor = "azul"
}

func chooseBestColor() -> String {
    guard #available(macOS 10.12, *) else {
        delvolver "gris"
    }
    let colors = ColorPreferense()
    devolver colores.bestColor
}

@available(macOS 10.12, *)
struct ColorPreference {
    var bestColor = "azul"
}

func chooseBestColor() -> String {
    guard #available(macOS 10.12, *) else {
        devolver "gris"
    }
    let colors = ColorPreference()
        devolver colores.bestColor
    }
}

@available(macOS 10.12, *)
struct ColorPreference {
    var bestColor = "azul"
}

func chooseBestColor() -> String {
    guard #available(macOS 10.12, *) else {
        devolver "gris"
    }
    let colors = ColorPreference()
        devolver colores.bestColor
}

@available(macOS 10.12, *)
struct ColorPreference {
    var BestColor = "azul"
}

func chooseBestColor() -> String {
    guard #available(macOS 10.12, *) else {
        return "gris"
    }
    let colors = ColorPreference()
        return colorres.bestColor
}

if #available(iOS 10, *) {
} else {
    // FallBack code
}

if #unavailable(iOS 10) {
    // FullBack Code
}

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
    Saludo de Regreso
}

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
}
print(greeting)

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
}

func greet(person: String) -> String {
    let greeting = "Hola, " + "Person "!"
}
print(greet(person: "Anna"))
//Prints Hello, Anna

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
}
print(greet(person: "Pedro"))
// Prints Hola, Pedro

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
}
print(person(Hola, "Pedro"))
//Prints Hola, Pedro

func greetAgaing(person: String) -> String {
    return "Hello againg, " + person "!"
}
print(greetAgain(person: Pedro))
// Prints Hello again, Pedro

func greetAgain(person: String) -> String {
    return "Hello, again, " + person ""!
}
print(greetAgain(person: "Pedro"))
//Prints Hello again, Pedro

func greetAgain(person: String) -> String {
    return "Hello again, " + person "!"
}
print(greetAgain(person: "Pedro"))
//Prints Hola again, Pedro

func greet(person: String) -> String {
    let greeting = "Hola, " + "persona "!"
}
print(person(Hola, "Pedro"))

func greetAgain(person: String) -> String {
    return "Hello again, " + person "!"
}
print(greetAgain(person: "Pedro"))
//Prints Hola again, Pedro

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHellWorld)
//Prints "hello, world"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld)
//Prints "hello, world"

func greet(person: String) -> String {
    let greeting = "Hola, " + "person "!"
}
print(person(Hola, Pedro))
//Prints Hola, "Pedro"

func sayHelloWorld(person: String) -> String {
    return "hello, world"
}
print(sayHelloWorld)
//Prints "hello, world"

func greetingAgain(person: String) -> String {
    let greetingAgain = "Hola, " + person "!"
}
print(greetingAgain(Hola, Pedro))
//Prints "Hola again, Pedro"

func greetingAgain(person: String) -> String {
    let greetingAgain = "Hola, " + person "!"
}
print(person(Hola, "Pedro"))
//Prints "Hola again, Pedro"

func sayHelloWorld(person: String) -> String {
    return "hello, world"
}
print(sayHelloWorld)

func greeting(person: String) -> String {
    let greeting = "Hola, " + "person "!"
}
print(person(Hola "Pedro"))
//Prints Hola Pedro

func greeting(person: String) -> String {
    let greeting = "Hola, " + "person "!"
}
print(person(Hola "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    let greeting = "Hola, " + "person "!"
}
print(person(Hola, "Pedro"))
//Prints "Hola Pedro"

func greet(person: String) -> String {
    return "Hola, " + "person "!"
}
print(person(Hola, "Pedro"))
//Prints Hola Pedro

func sayHelloWorld(person: String) -> String {
    return "hello, world"
}
print(sayHelloWorld)
//Prints Hello World

func greetAgain(person: String) -> String {
    let greetingAgain = "Hola, " + "person "!"
}
print(person(Hola, Pedro))
//Prints Hola again, Pedro

func greetAgain(person: String) -> String {
    return "hello, again " + "person "!"
}
print(greetAgain(person: "Pedro"))
//prints "Hello again, Pedro"

func greetAgain(person: String) -> String {
    return "hello,  again " + "person "!"
}
print(greetAgain(person: "Pedro"))
//Prints "hello again, Pedro"

func sayHelloWorld() -> String {
    return "Hello, World"
}
print(sayHelloWorld)

func sayHelloWorld() -> String {
    return "Hello, World"
}
print(sayHelloWorld)

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld)

fun greet(person: String) -> String {
    let greeting = "hola, " + "person "!"
}
print(person(Hola, Pedro))
//Prints "Hola, Pedro"

func greetAgain(person: String) -> String {
    let greetingAgain = "hola, " person "!"
}
print(greetAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print greet(person: "Pedro", alreadyGreeted: True)
//Prints "hello again, Pedro!"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True))
//Prints "hola again, "Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True))
//Prints "hello again, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True))
//Prints "hola again, Pedro"

func greet(person: String) {
    print("hello, \(greet)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String) {
    print("hello, \(greet)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alReadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: Pedro, alreadyGreeted: True))
//Prints "hello again, Pedro"

func greet(person: String) {
    print("hello, \(greet)")
}
greet(person: "Pedro")
//Prints Hola, Pedro

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld)

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld)

func greet(person: String) -> String {
    print("hello, \(greet)")
}
greet(person: "Pedro")
//Prints hello, Pedro

func printandCount(string: String) -> Int {
    print(string)
    return string.count
}
func printthoutCouting(string: String) {
    let _ = printThoutCouting(string: String)
}
printandCount(string: Hello, World)
//Print Hello, World and return a value of 12
printThoutCouting(string: hello, world)
//Prints hello, world but doesn't return a value

func greet(person: String) -> String {
    print("Hola, \(greet)")
}
greet(person: "Pedro")
//Prints Hola Pedro

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(person: "Pedro", alreadyGreeted: True)
//Prints "Hola again, Pedro"

func greet(person: String) -> String {
    let greeting = "Hola, " + "person "!"
}
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    let greetingAgain = "hola, " + person "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String) -> String {
    let greeting = "hola, " + "person "!"
}
print(greet(person: "Pedro"))
//Prints "hola, Pedro"

func greetingAgain(person: String) -> String {
    let greetingAgain = "hola, " + person + "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String) -> String {
    print("hola, \(greet)")
}
greet(person: "Pedro")
//Prints "hols, Pedro"

func greet(person: String, alreadyGreeting: Bool) -> String {
    if alreadyGreeting {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeting: True))
//Prints "hola, Pedro"

func printAndCount(string: String) -> Int {
    print(string)
    return string.count
}
printAndCount("hello, world")

func printAndCount(string: String) -> Int {
    print(string)
    return string.count
}
printAndCount("hello, world")

func printAndWithout(string: String) -> Int {
    let _ = printAndCount(string: String)
}
printAndCouting("hello, world")
//prints "hello, world" but does't return a value

func printAndCount(string: String) -> Int {
    print(string)
    return string.count
}
printAndCount("hello, world")

func printAndWiThout(string: String) -> Int (
    let _ = printAndCount(string: String)
)
printAndCouting("hello, world")
//Prints "hello, world, but does't not return a value"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value 
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMin = value
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
} 

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if currentMin < value {
            cuurentMin = value
        } else if currentMax > value {
            currentMax = value
        }
    }
    return (cuurentMin, currentMax)
}

func greet(person: String) -> String {
    let greeting = "hola, " + "person "!"
}
perin(greeting(person: "Pedro"))
//Prints "hola, Pedro"

func greetAgain(person: String) -> String {
    let grretingAgain = "hola, " + person "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String) -> String {
    print("hola, \(greet)")
}
greet(person: "Pedro")
//Prints "hola, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True))
//Prints "hola again, Pedro"
let bounds = minMax(array: [8, -6, -2, 0, 109, 3, 71])
print("min is \(bounds.min) and max is \(bound.max)")
//Print min es -6 y max es 10

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return.nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

if let bounds = minMax(array: [8, -6, 2, 109, 3, 71]) {
    print("min is \(bounds.in) and max is \(bounds.max)")
}
//Prints "min is -6 y max is 109"

func greeting(for person: String) -> String {
    "Hola, " + "person "!"
}
print(greeting(for: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(for person: String) -> String {
    "Hola, " + person "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "Hola, Pedro"

func greeting(person: String) -> String {
    "Hola, " + "Person "!"
}
print(greeting(for: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(person: String) -> String {
    return "Hola, " + "person "!"
}
print(greeting(for: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(person: String) -> String {
    return "Hola, " + person "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String, de la ciudad natal: String) -> String {
    return "Hola \(person)! me alegro que me pudieras visitar a \(ciudad natal) "
}
print greet(person: "Pedro", of: Cupertino)
//Prints "Hola, Pedro que bien que me pudiste visitar en Cupertino"

func greet(person: String, de la ciudad natal: String) -> String {
    return "Hola \(person) me alegro que me pudieras visitar a \(ciudad natal)"
}
print greet(person: "Pedro", of: Cupertino)
//Prints "Hola, Pedro que bien que me pudiste visitar a Cupertino"

func greet(person: String, de la ciudad natal: String) -> String {
    return "Hola \(person) que bien que viniste a visitarme a \(ciudad natal)" 
}
print greet(person: "Pedro", of: Cupertino)
//Prints "Hola, Pedro que bien que viniste a visitarnos a Cupertino"

func greet(person: String, alreadyGreeted: Boll) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True)
//Prints "Hola again, Pedro"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentmax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (cirrentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentmax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty { return nil}
    for value in array[1..<array.count] {
        if value < cirrentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMix, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func greet(person: String) -> String {
    let greeting = "hola, " + "person, "!"
}
print(greeting(person: "Pedro"))
//Prints "hola, Pedro"

func greet(person: String) -> String {
    let grettingAgain = "hola, " + person "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func sayHelloWorld(person: String) -> String {
    print(sayHelloWorld)
}
//Prints "Hello, World"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return (person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: True))
//Prints "hola again, Pedro"

func saHelloWorld() -> String {
    print(sayHelloWorld)
}
//Prints "Hello, World"

func greet(person: String) -> String {
    print("hola,  \(greet)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    print("hola,  \(greet)")
}
greet(person: "Pedro")
//Prints "hola, Pedro"

func greet(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            value = currentMin
        } else if value > currentMax {
            value = currentMax
        }
    }
    return (currentMin, currentMax)
}

func greet(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    if value < currentMin {
        value = currentMin
    } else if value > currentMax {
        value = currentMax
    }
    return (currentMin, currentMax)
}

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "hello, world")
//Prints "hello, world y devuelve un valor de 12"

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "hello, world")
//Prints "hello, world" y devuelve un valor de 12

func printThoutCounting(string: String) {
    let _ = printAndThoutCounting(string: String)
}
printAndThoutCountinng(string: "hello, world")
//Prints "hello, world" pero no devulve un valor 

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
print(string: "hello, world")
//Prints "hello, world y devuelve un valor 12"

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "hello, world")
//Prints "hello, world" y devuelve un valor 12

func printAndThoutCouting(string: String) {
    print(string)
}
printAndThoutCouting(string: "hello, world")
//Prints "hello, world" y no devulve ningun valor

func printAndThoutCouting(string: String) {
    print(string)
}
printAndThoutCounting(string: "hello, world")
//Prints "hello, world" y no devuelve ningun valor

func printAndCount(string: String) -> String {
    print(string)
    return string.count
}
printAndCount(string: "hello, world")
//Prints "hello, world" y devuelve un valor 12

func printAndThoutCounting(string: String) {
    print(string)
}
printAndThoutCouting(string: "hello, world")
//Prints "hello, world" y no devuelve un valor

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array: [8, -6, 2, 109, 3, 71])
print("min is  \(bounds.min) and max is  \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = Array[0]
    var currentMin = Array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is  \(bounds.min) and max is  \(bounds.max) ")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array: [8, -6, 2, 109, 3, 71])
print("min is  \(bounds.min) and max is  \(bounds.max)")
//Prints "min es -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bouns.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int)? {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
if let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greet(person: String) {
    print(string)
}
greet(person: "Pedro")
//Prints "hello, Pedro"

func greet(person: String) -> String {
    print("hello, \(person)")
}
greet(person: "Pedro")
//Prints "hello, Pedro"

func greet(person: String) -> String {
    let greeting = "hola, " + "person "!"
}
print(greeting(person: "Pedro"))
//Prints "hello, Pedro"

func greetingAgain(person: String) -> String {
    let greetingAgain = "Hola, " + person "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadurGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print greet(person: "Pedro", alreadyGreeted: True)
//Prints "hola, Pedro"

func greet(string: String) -> String {
    print(string)
}
print greet(string: "Pedro")
//Prints "Hola, Pedro" y no devuelve ningun valor

func greet(string: Int) -> String {
    print(string)
}
print greet(string: "Pedro")
//Prints "Hols, Pedro" y devuelve un valor de 12 

func sayHelloWorld(string: String) {
    print(sayHelloWorold)
}
//Prints "Hello, World"

func greet(person: String) -> String {
    print("hello, \(greet)")
}
greet(person(hello, "Pedro"))
//Prints "hello, Pedro"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bound = minMax(array: [8, -6, 2, 109, 3, 71])
print("min is \(bound.min) and max is \(bound.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bound = minMax(array: [8, -6, 2, 109, 3, 71])
print("min is \(bound.min) and max is \(bound.max)")
//Prints "min is -6 and max is 109"

let greet(person: String) -> String {
    let greeting = "hola, " + "person "!"
}
print(greeting(person: "Pedro"))
//Prints "hola, Pedro"

let greet(person: String) -> String {
    let greetingAgain = "hola, " + person "!"
}
print(greetingAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greet(person: String) -> String {
    let _ = "hola, " + person "!"
}
print(greet(person: "Pedro"))
//Prints "hols, Pedro"

func greetAgain(person: String) -> String {
    return "hola again, " + person + "!"
}
print(greetAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func greetAgain(person: String) -> String {
    return "hola, " + person + "!"
}
print(greetAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func sayHelloWorld() -> String {
    return sayHelloWorld()
}
print(sayHelloWorls())
//Prints "hello, world"

func sayHelloWorld() -> String {
    return sayHelloWorld
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hola again, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hola again, Pedro"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentmax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value un array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hello again, Pedro"

func sayHelloWorld() -> String {
    return sayHelloWorld()
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(string: String) {
    print(string)
}
greet(string: "Pedro")
//Prints "hello Pedro" y no devuelve ningun valor

func greet(string: String) -> int {
    print(string)
}
greet(string: "Pedro")
//Prints "hello Pedro" y devuelve un valor 12

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hola again, Pedro"

func sayHelloWorld() -> String {
    print(syHelloWorld())
}
//Prints "hello world"

func printAndCount(string: String) -> Int {
    print(string)
    return string.count
}
printAndCount(string: "Pedro")
//Prints "hello, Pedro" y devuelve un valor 12

func printAndCounting(string: String) {
    let _ = printAndCouting(string: String)
}
printAndCouting(string: "Hello, Pedro")
//Prints "Hello, Pedro" y no devuelve ningun valor 

func printAndCouting(string: String) {
    let _ = printAndCouting(string: String)
}
printAndCouting(string: "Hello, Pedro")
//Prints "Hello, Pedro" y no devuelve ningun valor 

func greeting(for: String) -> String {
    "Hola, " + person + "!"
}
print(greeting(for: Pedro))
//Prints "Hola, Pedro"

func anotherGreeting(for: String) -> String {
    return "Hola, " + person + "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "Hola, Pedro"

func greeting(for: String) -> String {
    "Hola, " + person + "!"
}
print(greeting(for: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(for: String) -> String {
    return "Hola, " + person + "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    let greeting = "hola, " + "person" + "!"
}
print(greet(person: "Pedro"))
//Prints "hola, pedro"

func greet(person: String) -> String {
    return "hola, " + person + "+"
}
print(greet(person: "Pedro"))
//Prints "hola, Pedro"

func greet(person: String) -> String {
    let greetingAgain = "hola, " + person + "!"
}
print(greetAgain(person: "Pedro"))
//Prints "hola again, Pedro"

func sayHelloWorld() -> String {
    print(sayHelloWorld())
}
//Prints "hello, World"

func printAndCount(for: String) {
    let _ = printAndCount(for: String)
}
print(printAndCount(for: "Pedro"))
//Prints "hola, pedro" y no devulve ningun valor

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreaduGreeted: true))
//Prints "hola again, Pedro"

func minMax(array: [Int]) -> (min: Int, min: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array[Int]) -> (min: Int, Max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greeting(for: String) -> String {
    "hola, " + person + "!"
}
print(greeting(for: "Pedro"))
//Prints "hola, Pedro" y no devulve ningun valor

func anotherGreeting(for: String) -> String {
    return "hola, " + person + "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "hola, Pedro" 

func greeting(for: String) -> String {
    "Hola, " + person + "!"
}
print(greeting(for: "Pedro"))
//Prints "hola, Pedro"

func anotherGreeting(for: String) -> String {
    return "hola, " + person + "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "hola, Pedro"

func greet(person: String, ciudad natal: String) -> String {
    return "hola \(person)! me alegro que hayas venido a mi \(ciudad natal)"
} 
print greet(person: "Pedro", of: "Cupertino")
//Prints "hola, Pedro! me alegro que me hayas venido a visitar desde Cupertino"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person)! me alegro que me hayas venido a visitar desde \(ciudad natal)"
}
print(greet(for: "Pedro", of: Cupertino))
//Prints "Hola Pedro! me alegro que me hayas venido a visitar desde Cupertino"

func greeting(person: String, ciudad natal: String) -> String {
    return "Hola \(person) me alegro que me hayas venido a visitar desde \(ciudad natal)"
}
print(greeting(for: "Pedro", of: Cupertino))
//Prints "Hola Pedro! me alegro que hayas venido a visitarme desde Cupertino"

func greeting(for: string, ciudad natal: String) -> String {
    return "Hola \(person) gracias por venir a visitarme a mi \(ciudad natal)"
}
print(greeting(for: "Pedro", of: Cupertino))
//Prints "Hola Pedro! gracias por venir a visitarme desde Cupertino"

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Doble = 0
    for numero in numeros {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints devuelve 3.0 que es la media aritmetica de estos 5 numeros
aritmeticaMedia(3, 8.25, 18.25)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros 

func arithmeticanMean(_ numbres: Double ...) -> Double {
    var total: Double = 0
    for numeros in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticaMedia(3, 8.25, 18.75)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Double = 0
    for numeros in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticanMedia(3, 8.25, 18.75)

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Double = 0
    for numbers in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
aritmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros 
arithmeticanMedia(3, 8.25, 18.75)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Double = 0
    for numbers in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la aritmetica de estos 5 numeros
aritmeticanMedia(3, 8.25, 18,75)
//Prints Devuelve 3.o que es la aritmetica de estos 3 numeros

func arithmeticanMean(_ numbers: Doble ...) -> Double {
    var total: Double = 0
    for numbers in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la aritmetica de estos 5 numeros 
arithmeticaMedia(3, 8.25, 18.75)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Double = 0
    for numbers in numero {
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticanMedia(3, 8.25, 18.75)

func arithmeticanMean(_ numbers: Double ...) -> Double {
    var total: Double = 0
    for numbers in numero{
        total += numero
    }
    return total / Double(numbers.count)
}
arithmeticanMean(1, 2, 3, 4, 5)
//Prints Devuelve 3.0 que es la arithmetica de estos 5 numeros
arithmeticanMedia(3, 8.25, 18.75)
//Prints Devuelve 3.0 que es la arithmetica de estos 3 numeros

func greeting(for: String) -> String {
    "Hola, " + person + "!"
}
print(greeting(for: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(for: String) -> String {
    return "Hola, " + person + "!"
}
print(anotherGreeting(for: "Pedro"))
//Prints "Hola, Pedro"

func awapTwoInts(_ a: inout Int, _ b: inout Int) {
    let temporaryA = a
    a = b
    b = temporaryA
}
print(awapTowoInts)

func awapTwoInts(_ a: inout Int, _ b: inouts Int) {
    let temporaryA = a
                a = b
                b = temporaryA
}
print(awapTwoInts)

func awapTwoInts(_ a: inout Int, _ b: inout Int) {
    temperaryA = b
            a = b
            b = temperaryA
}
print(awapTwoInts)

func awapTwoInts(_ a: inout Int, _ b: inout Int) {
    temperaryA = b
            a = b
            b = temperaryA
}
print(awapTwoInts)

var someInt = 3
var anotherInt = 107
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(someInt)")
//Prints "someInt is 3 and anotherInt is 103"

var someInt = 3
var anotherInt = 107
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is 3 and anotherInt is 103"

var someInt = 3
var anotherInt = 109
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is 3 and anotherInt is 103"

func greet(person: String) -> String {
    "Hola, " + "Person" + "!"
}
greet(person: "Pedro")
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    return "Hola, " + person + "!"
} 
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func sayHelloWorld() -> String {
    print(sayHelloWorld())
}
//Prints "Hello, World"

func greet(person: String, alreaddyGreeted: bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "Hola again, Pedro"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin: array[0]
    var currentMax: array[0]
    for value in array[1..<array.count] {
        value < currentMin {
            currentMin = value
        } else if value > currentMin {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minmax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greet(person: String) -> String {
    let greeting = "Hola, " + person + "!"
    return greeting
}
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    let greeting = "Hola, " + person + "!"
    return greeting
}
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func greetAgain(person: String) -> String {
    return "Hola, " + person + "!"
}
print(greetAgain(person: "Pedro"))
//Prints "Hola again, Pedro"

func sayHelloWorld -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(person: String) -> String {
    print("hola, \(person)")
}
greet(person: "Pedro")
//Prints "hola, Pedro"

func greet(person: String) -> String {
    print("hola, \(person)")
}
greet(person: "Pedro")
//Prints "hola, Pedro"

func printAndCount(string: String) -> int {
    perint(string)
    return string.count
}
printAndCount(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devulve un valor 12

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devuelve un valor 12 

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devuelve un valor 12

func printAndCouting(string: String) {
    let _ = printAndCounting(string: String)
}
printAndCouting(string: "Hola, Muendo")
//Prints "Hola, Mundo" y no devuelve ningun valor

func printAndCouting(string: String) {
    let _ = printAndCounting(string: String)
}
printAndCouting(string: "Hola, Mundo")
//Prints "Hola, Mundo" y no devuelve ningun valor

func printAndCouting(string: String) {
    let _ = printAndCouting(string: String)
}
printAndCouting(string: "Hola, Mundo")
//Prints "Hola, Mundo"

func printAndCount(string: String) -> int {
    print(string)
    return string.count
}
printAndCount(string: "Hola, Mundo")
//Prints "Hola, Mundo" y devuelve un valor 12

func printAndCouting(string: String) {
    let _ = printAndCouting(string: String)
}
printAndCouting(string: "Hola, Mundo")
//Prints "Hola, Mundo" y no devuelve ningun valor

func greet(person: String) -> String {
    let greeting = "Hola, " + person + "!"
}
print(greeting(person: "Pedro"))
//Prints "Hola, Pedro"

func greet(person: "Pedro") -> String {
    return "Hola, " + person + "!"
}
print(greet(person: "Pedro"))
//Prints "Hola, Pedro"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hola again, Pedro"

func anotherCounting(string: String) {
    let _ = anotherCouting(string: String)
}
print(anotherCouting(string: "hola, Pedro"))
//Prints "hola, Pedro" y no devuleve ningun valor

func anotherThoutCouting(string: String) -> int {
    print(string)
    return string.count
}
anotherThoutCouting(string: "hello, World")
//Prints "hello, world y devuelve un valor 12"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = array[8, -6, 2, 109, 3, 71]
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = array[8, -6, 2, 109, 3, 71]
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func greet(person: String) -> String {
    let greeting = "Hola, " + "person" + "!"
}
greeting(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    return "Hola, " + person + "!"
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: person)
    } else {
        return greet(person: person)
    }
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "Hola again, Pedro"

func sayHelloWorld() -> String {
    return "hello world"
}
print(sayHelloWorld())
//Prints "hello world"

func printAnotherCouting(string: String) {
    let _ = printAnotherCouting(string: string)
}
printAnotherCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y no devuelve ningun valor

func printThoutCouting(string: String) -> int {
    print(string)
    return string.count
}
printThoutCouting(string: "Pedro")
//Prints "Hola, Pedro" y devuelve un valor 12

func printThoutCouting(string: String) -> int {
    print(string)
    return string.count
}
printThoutCounting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devuleve un valor 12

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
    for value in array[1..<array.count] {
        if value < currentMin {
            currentMin = value
        } else if value > currentMax {
            currentMax = value
        }
    }
    return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds. max)")
//Prints "min is -6 and max is 109"

func greet(person: String) -> String {
    print("Hola, \(person)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    print("Hola, \(person)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func greet(person: String) -> String {
    print("Hola, \(person)")
}
greet(person: "Pedro")
//Prints "Hola, Pedro"

func printAndCouting(string: String) -> String {
    print(string)
    return string.count
}
printAndCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devulve una valor 12

func printWithToutCouting(string: String) {
    let _ = printWithToutCouting(string: String)
}
printThoutCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y no devulve ningun valor

func printAndCouting(string: String) -> int {
    print(string)
    return string.count
}
printAndCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devuelve un valor 12

func printWithThoutCouting(string: String) {
    let _ = printWithThoutCouting(string: String)
}
printWithThoutCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y no devulve ningun valor

func greeting(for person: String) -> String {
    "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    return "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    return "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    return "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func printAndCouting(string: String) -> int {
    print(string)
    return string.count
}
printAndCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y devuelve una valor 12

func printWithThoutCouting(string: String) {
    let _ = printWithThoutCouting(string: String)
}
printWithThoutCouting(string: "Pedro")
//Prints "Hola, Pedro"

func greeting(for person: String) -> String {
    "Hola, " + person + "!"
}
greeting(for person: "Pedro")
//Prints "Hola, Pedro"

func greetingAgain(for person: String) -> String {
    return "Hola, " + person + "!"
}
greetingAgain(for person: "Pedro")
//Prints "Hola, Pedro"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person) es un gusto tenerte en mi \(ciudad natal)"
}
greet(person: "Pedro", of: Cupertino)
//Prints "Hola Pedro es un gusto que me hayas venido a visitar a Cupertino"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person) es un gusto que me hayas venido a visitar desde \(ciudad natal)"
}
greet(person: "Pedro", of: Cupertino)
//Prints "Hola Pedro es un gusto que me hayas venido a visitar desde Cupertino"

func arithmeticanMean(_ Double: numbers ...) -> Double {
    var total: Double = 0
    for numero in numbers {
        total += numero
    }
    return total / Double[numbers.count]
}
arithmeticanMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticanMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numbers ...) -> Double {
    var total: Double = 0
    for numero in numbers {
        total += numero
    }
    return total / Double[numbers.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devulve 3.0 que es la aritmetica de estso 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numbers ...) -> Double {
    var total: Double = 0
    for numero in numbers {
        total += numero
    }
    return total / Double[numbers.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numbers ...) -> Double {
    var total: Double = 0
    for numero in numbers {
        total += numero
    }
    return total / Double[numbers.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Prints Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numeros ...) -> Double {
    var total: Double = 0
    for numero in numeros {
        total += numeros 
    }
    return total / Double[numeros.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuleven 3.0 que es la aritmetica de estos 5 numeros
aritmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numeros...) -> Double {
    var total: Double = 0
    for numero in numeros {
        total += numeros
    }
    return total / Double[numeros.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la arithmetica de estos 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

var someInt = 3
var anotherInt = 107
swatTwoInt(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is now 107 and anotherInt is now 3"

var someInt = 3
var anotherInt = 107
swatTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Print "someInt is now 103 and anotherInt is now 3"

var someInt = 3
var anotherInt = 103
swaTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is now 103 and anotherInt is now 3"

var someInt = 3
var anotherInt = 103
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(aotherInt)")
//Prints "someInt is now 103 and anotherInt is now 3"

func addTwoInts(_ a: Int, _ b: Int) -> Int {
    return a + b
}
print(addTwoInts)

func multiplyTwoInts(_ a: Int, _ b: Int) -> Int {
    return a * b
}
print(multiplyTwoInts)

func addTwoInts(_ a: Int, _ b: Int) -> Int {
    return a + b
}
print(addTwoInts)

func multiplyTwoInts(_ a: Int, _ b: Int) -> Int {
    return a * b
}
print(multiplyTwoInts)

var matchFuntion: (Int, Int) -> Int = addTwoInts

func addTwoInts(5: Int, 5: Int) -> Int {
    return 5 + 5
}
print("resultado: \(matchFuntion(5, 5))")
//Prints "Resultado es 10"

var matchFuntion: (Int, Int) -> Int = addTwoInts

func addTwoInts(5, 5) -> Int {
    return 5 + 5
}
print("Resultado: \(matchFuncion(5, 5))")
//Prints "Resultado es 10"

var matchFuntion: (Int, Int) -> Int = multiplyTwoInts

func multiplyTwoInts(5, 5) -> Int {
    return 5 *5
}
print("Resultado: \(matchFuntion(5, 5))")
//Prints "Resultado es 25"

var matchFuntion: (Int, Int) -> Int = addTwoInts

func addTwoInts(5, 5) -> Int {
    return 5 + 5
}
print("Resultado: \(matchFuntion(5, 5))")
//Prints "Reltado es 10"

var matchFuntion: (Int, Int) -> Int = addTwoInts

func addTwoInts(5, 5) -> Int {
    return 5 + 5
}
print("Resultado: \(matchFuntion(5, 5))")
//Prints "Resultado es 10"

var matchFuntion: (Int, Int) -> Int = multiplyTwoInts

func multiplyTwoInts(5, 5) -> Int {
    return 5 * 5
}
print("Resultado: \(matchFuntion(5, 5))")
//Prints "El resultado es 25"

var matchFuntion: (Int, Int) -> Int = divideTwoInts

func divideTwoInts(4, 2) -> Int {
    return 4 / 2
}
print("Resultado: \(matchFuntion(4, 2))")
//Prints "Resultado es 2"

var matchFuntion: (Int, Int) -> Int = addTwoInts 

func printMatchResult(_ matchFuntion: (Int, Int), _ a: Int, _ b: Int) {
    print("Resultado: \(matchFuntion(5, 5))")
}
//Prints "Resultado es 10"

var matchFuntion: (Int, Int) -> Int = addTwoInts {
    print("Resultado: \(matchFunction(2, 3))")
}
//Prints "Resultado es 5"

var matchFuntion: (Int, Int) -> Int = multiplyTwoInts {
    print("Resultado: \(matchFuntion(2, 3))")
}
//Prints "Resultado es 6"

matchFuntion = multiplyTwoInts
print("Resultado: \(matchFuntion(2, 3))")
//Prints "Resultado es 6"

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5))
//Prints "Resultado es 8"

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//"Resultado es 8"

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func printMatchResult(_ matchFuntion: (Int, Int) -> Int, _ a: Int, _ b: Int) {
    print("Result: \(matchFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

var matchFuntion(Int, Int) -> Int = addTwoInts

func addTwoInts(Int, Int) -> Int {
    print("Resultado: \(matchFuntion(5, 5))")
}
//Prints "Resultado es 10"

var matchFuntion(Int, Int) -> Int = multiplyTwoInts

func multiplyTwoInts(Int, Int) -> Int {
    print("Resultado: \(matchFuntion(5, 5))")
}
//Prints "Resultado es 25"

var matchFuntion(_ a: Int, _ b: Int, Int, Int) -> Int = addTwoInts

func addTwoInts(_ a: Int, _ b: Int, Int, Int) -> Int {
    print("Resultado: \(matchFuntion(5, 5))")
}
matchFuntion(addTwoInts, 5, 5)
//Prints "Resultado es 10"

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func stepForward(_ input: Int) -> Int {
    input + 1
}

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func chooseStepFuntion(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward: stepForward
}

func shooseStepForward(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward, stepForward
}

func shooseStepForward(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward: stepForward
}

func shooseStepBackward(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward: stepFroward
}

func shooseStepBackward(backward: Bool) -> (Int) -> Int {
    return backward ?, stepBackward: stepForward
}

func shooseStepBackward(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward: stepForward
}

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func stepForward(_ input: Int) -> Int {
    return input + 1
}

func shooseStepBackward(backward: Bool) -> (Int) -> int {
    return backward ?, stepBackward: stepForward
}
func stepBackward(_ input: Int) -> Int {
    return input + 1
}

func shooseStepBackward(backward: Bool) -> (Int) -> int {
    backward ?, stepBackward: stepForward
}

var currentValue = 0
let nearerToZero = chooseStepFuntion(backward: currentValue > 0)

var matchFuntion(Int, Int) -> Int = addTwoInts

func addTwoInts(Int, Int) -> Int {
    print("Resultado: \(matchFuntion(5, 5))")
}
//Prints "Resultado es 10"

func stepBackward(_ input: Int) -> Int {
    return input + 1
}

func stepBackward(_ input: Int) -> Int {
    return input + 1
}

func shooseStepBackward(backward: Bool) -> Int {
    return backward ?, stepBackward: stepForward
}

var matchFuntion(Int, Int) -> Int = multiplyInts

func multiplyInts(Int, Int) -> Int {
    print("Resultado: \(matchFuntion(5, 5))")
}
//Prints "Resultado es 25"

var matchFuntion(_ a: Int, _ b: Int, (Int, Int)) -> Int = addTwoInts

func printMatchResult(_ a: Int, _ b: Int, (Int, Int)) -> Int {
    print("Resultado: \(matchFuntion(a, b))")
}
printMatcResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

func stepBackward(_ input: Int) -> Int {
    return input + 1
}

func shooseStepBackward(backward: Bool) -> Int {
    return backward ?, stropBackward: stepForward
}

var matchFuntion(_ a: Int, _ b: Int, (Int, Int)) -> Int = multiplyInts

func printMatchResult(_ a: Int, _ b: Int, (Int, Int)) -> Int {
    print("Resultado: \(matchFuntion(a, b))")
}
printMatchResult(multiplyInts, 5, 5)
//Prints "Resultado es 25"

var matchFuntion(_ a: Int, _ b: Int, (Int, Int)) -> Int = divideInts

func printMatchResult(_ a: Int, _ b: Int, (Int, Int)) -> Int {
    print("Resultado: \(matchFuntion(a, b))")
}
printMatchResult(divideInts, 25, 5)
//Prints "Resultado es 5"

var currentValue = 0
let moveNeanerToZero: chooseStepForward(backward: currentValue > 0)
print("Contando hasta 0:")
while currentValue != 0 {
    print("\(currentValue)... ")
    currentValue = moveNeanerToZero(currentValue)
}    
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
print("Contando hasta 0:")
while currentValue ! = 0 {
    print("\(currentValue)... ")
    currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
print("Contando hasta 0:")
    while currentValue ! = 0 {
        print("\(currentValue)... ")
        currentValue = moveNeanerToZero(currentValue)
    }
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentvalue > 0)
    print("!Contando hasta 0:!")
    while currentValue ! = 0 {
        print("\(currentValue)...")
        currentValue = moveNeanerToZero(currentValue)
    }
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
    print("!Contando hasta 0:!")
    while currentValue ! = 0 {
        print("\(currentValue)...")
        currentValue = moveNeanerToZero(currentValue)
    }
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > o)
    print("!Contando hasta 0:!")
while currentValue ! = 0 {
    print("\(currentValue)...")
    currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentvalue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
    print("Contando hasta 0:")
while currentValue ! = 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print(!Cero!)
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward" currentValue > 0)
    print("Contando hasta 0:")
while currentValue ! = 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
    print("Contando hasta 0:")
while currentValue ! = 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    3...
    2...
    1...
!Cero!

var currentValue = 0
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
    print("Contando hasta 0:")
while currentValue ! = 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    3...
    2...
    1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBorward(input: Int) -> return {input - 1}
    return backward ?, stepBackward: stepForward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
// moveNeanerToZero no se refiere a stepForward() funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print(!Cero!)
    -4
    -3
    -2
    -1
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input - 1}
    return backward ?, stepForward: stepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
//moveNeanerTozero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -< return {input - 1}
    return backward ?, atepForward: stepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
//moveNeanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
curretnValue = moveNeanerToZero(cuurentValue)
}
print("!Cero!")
    // -4
    // -3
    // -2
    // -1
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input - 1}
    return backward ?, stepForward: strpBackward
}
var currentvalue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
//moveNeanerToZero no se refiere a stepForward() funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = stepForward(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input - 1}
return backward ?, stepForward: stepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
//moveNeanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = stepForward(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: int) -> return {input + 1}
    func stepBackward(input: int) -> return {input - 1}
return backward ?, stepForward: stepBackward
}
var currentValue = -4
let moveLeanerToZero = shooseStepForward(backward: currentValue > 0)
// moveLeanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = moveLeanerToZero(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input - 1}
return backward ?, stepForward: stepBackWard
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
// moveNeanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = shooseStepForward
}
print("!Zero!")
    // -4...
    // -3...
    // -3...
    // -2...
    // -1...
!Zero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input -1}
return backward ?, stepForward: stepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
// moveneanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    print("\(currentValue)...")
currentValue = shooseStepForward
}
print("!Zero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Zero!

func greeting(person: String) -> String {
    print("Hola, \(person)")
}
greeting(person: "Pedro")
// "Hola, Pedro"

func sayHelloWorld() -> String {
    return "hello, world"
}
print(sayHelloWorld())
//Prints "hello, world"

func greet(person: String, alreadyGreeted: Bool) -> String {
    if alreadyGreeted {
        return greetAgain(person: persoon)
    } else {
        return greet(person: person)
    }
}
print(greet(person: "Pedro", alreadyGreeted: true))
//Prints "hola again, Pedro"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
for value in array[1..<array.count] {
    if value < currentMin {
currentMIn = value
    } else if value > currentMax {
currentMax = value
    }
}
return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min)" and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
for value in array[1..<array.count] {
    if value < currentMin {
currentMin = value
    } else if value > currentMax {
currentmax = value
    }
}
return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
for value in array[1..<array.count] {
    if value < currentMin {
currentMin = value
    } else if value > currentMax {
currentMax = value
    }
}
return (currentMin, currentMax)
}
let bounds = minMax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func minMax(array: [Int]) -> (min: Int, max: Int) {
if array.isEmpty {return nil}
    var currentMin = array[0]
    var currentMax = array[0]
for value in array[1..<array.count] {
    if value < currentMin {
currentMin = value
    } else if value > currentMax {
currentMax = value
    }
}
return (currentMin, currentMax)
}
let bounds = minmax(array[8, -6, 2, 109, 3, 71])
print("min is \(bounds.min) and max is \(bounds.max)")
//Prints "min is -6 and max is 109"

func printAndCouting(string: String) -> int {
    print(string)
    return string.count
}
printAndCouting(string: "Pedro")
//Prints "Hola, Pedro" y devuelve un valor 12

func printWithThoutCouting(string: String) {
    let _ = printWithThoutCouting(string: String)
}
printAndWithThoutCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro" y no devuleve ningun valor

let printWithThoutCouting(string: String) {
    let _ = printWithThoutCouting(string: string)
}
printWithThoutCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro"

let printWithThoutCouting(string: String) {
    let _ = printWithToutCouting(string: String)
}
printWithThoutCouting(string: "Hola, Pedro")
//Prints "Hola, Pedro"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person) gracias por venir a visitarme desde \(ciudad natal)")
}
print(greet(person: "Pedro", of: Cupertino))
//Prints "Hola Pedro muchas gracias por venir a visitarme a Cupertino"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person) muchas gracias por venir a visitarme desde \(ciudad natal)"
}
print(greet(person: "Pedro", of: Cupertino))
//Prints "Hola Pedro muchas gracias por venir a visitarme desde Cupertino"

func greet(person: String, ciudad natal: String) -> String {
    return "Hola \(person) gracias por venir a visitarnos a \(ciudad natal)"
}
print(greet(person: "Pedro", of: Cupertino))
//Prints "Hola Pedro muchas gracias por venir a visitarnos desde Cupertino"

func greeting(person: String) -> String {
    let greeting = "hola, " + person + "!"
}
print(greeting(person: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(person: String) -> String {
    return "Hola, " + person + "!"
}
print(anotherGreeting(person: "Pedro"))
//Prints "Hola, Pedro"

func anotherGreeting(for person: "Pedro") -> String {
    return "Hola, " + person + "!"
}
print(anotherGreeting(for person: "Pedro"))
//Prints "Hola, Pedro"

func shooseStepForward(backward: currentValue > 0) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepBackward(input: Int) -> return {input - 1}
return backward ?, stepForward: StepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
// moveNeanerToZero no se refiere a stepForward Funtion
while currentValue > 0 {
    currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func shooseStepForward(backward: Bool) -> (Int) -> Int {
    func stepForward(input: Int) -> return {input + 1}
    func stepbackward(input: Int) -> return {input - 1}
return backward ?, stepForward: stepBackward
}
var currentValue = -4
let moveNeanerToZero = shooseStepForward(backward: currentValue > 0)
//moveNeanerToZero no se refiere a stepForward() Funtion
while currentValue > 0 {
    currentValue = moveNeanerToZero(currentValue)
}
print("!Cero!")
    // -4...
    // -3...
    // -2...
    // -1...
!Cero!

func arithMean(_ Double: numeros ...) -> Double {
    var total: Double = 0
for numeros in numero {
    total += numeros
}
return total / Double[numeros.count]
}
arithMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

func arithmeticMean(_ Double: numeros...) -> Double {
    var total: Double = 0
for numeros in numero {
    total += numeros
}
return total / Double[numeros.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

var printMatchResult(_ a: Int, _ b: Int, (Int, Int)) -> Int = addTwoInts

func matchFuntion(_ a: Int, _ b: Int, (Int, Int)) -> Int {
    print("Resultado: \(macthFuntion(a, b))")
}
printMatchResult(addTwoInts, 3, 5)
//Prints "Resultado es 8"

var printMatchResult(_ a: Int, _ b: Int, (Int, Int)) -> Int = multiplyInts

func matchFuntion(_ a: Int, _ b: Int, (Int, Int)) -> Int {
    print("Resultado: \(matchFuntion(a, b))")
}
printMatchResult(multiplyInts, 5, 5)
//Prints "Resultado es 25"

func arithmeticMean(_ Double: numeros...) -> Double {
    var total: Double = 0
for numero in numeros {
    total += numero
}
return total / Double[numeros.count]
}
arithmeticMean(1, 2, 3, 4, 5)
//Devuelve 3.0 que es la aritmetica de estos 5 numeros
arithmeticMean(3, 8.25, 18.75)
//Devuelve 3.0 que es la aritmetica de estos 3 numeros

var someInt = 3
var anotherInt = 107
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is 107 and anotherInt is 3"

var someInt = 3
var anotherInt = 107
swapTwoInts(&someInt, &anotherInt)
print("someInt is now \(someInt) and anotherInt is now \(anotherInt)")
//Prints "someInt is now 107 and anotherInt is now 3"

func shooseStepFuntion(backward: Bool) -> (Int) -> Int {
    return backward ?, stepForward: stepBackward
}
var currentValue = 0
let moveNeanerToCero = shooseStepFuntion(backward: currentValue > 0)
//moveNeanerToCero no se refiere a stepForward() Funtion
print("Contando hasta Cero")
    while currentValue ! = 0 {
        print("\(currentValue)...")
    }
print("!Cero!")
    // 3...
    // 2...
    // 1...
    // Cero!

func shooseStepFuntion(backward: Bool) -> (Int) -> Int {
    return backward ?, stepForward: stepBackward
}
var currentValue = 0
let moveNeanerToCero = shooseStepFuntion(backward: currentValue > 0)
//moveNeanerToCero no se refiere a stepForward() funtion
print("Contando hasta Cero")
    while currentValue ! = 0 {
        print("\(currentValue)...")
    }
print("Cero")
    // 3...
    // 2...
    // 1...
    // Cero!

let nombres = ["Chris", "Alex", "Eva", "Barry", Daniella]

let nombres = ["Chris", "Alex", "Eva", "Barry", "Daniella"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}
var reversedNombres = nombres.sorted(by: backward)
// reversedNombres es igual a ["Eva", "Daniela", "Chris", "Barry", "Alex"]

let nombres = ["Pedro", "Juan", "Oscar", "Anna", "Carlos"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by: backward)
// reversedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Pedro", "Juan", "Oscar", "Carlos", "Anna"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by: backward)
// reversedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Pedro", "Juan", "Oscar", "Anna", "Carlos"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by; backward)
// reversedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", Anna]

let nombres = ["Oscar", "Pedro", "Anna", "Carlos", "Juan]

func bacward(_ s1: String, _ s2_ String) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by: backward)
// reversedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Oscar", "Anna", "Pedro", "Carlos", "Juan"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by: backward)
//reservedNumeros es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Juan", "Pedro", "Oscar", "Carlos", "Anna"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

reservedNombres = nombres.sorted(by: { (s1: String, s2: String) -> Bool in
    return s1 > s2
    })

let nombres = ["Oscar", "Pedro", "Anna", "Carlos", "Juan"]

func backguard(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

reservedNombres = nombres.sorted(by: { (s1: String, s2: String) -> Bool in
    return s1 > s2
    })

let nombres = ["Juan", "Anna", "Oscar", "Pedro", "Carlos"]

func backward(_ s1: String, _ s2: Int) -> Bool {
    return s1 > s2
}

var reversedNombres = nombres.sorted(by: backguard)
//reversedNombres es igual a ("Pedro", "Oscar", "Juan", "Carlos", "Anna")

let nombres = ["Anna", "Oscar", "Jose", "Carlos", "Pedro"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reservedNombres = nombres.sorted(by: backward)
//reservedNombres es igual a ["Pedro", "Oscar", "Jose", "Carlos", "Anna"]

let nombres = ["Pedro", "Oscar", "Jose", "Anna", "Carlos"]

func backward(_ s1: String, _s2: String) -> Bool {
    return s1 > s2
}

reversedNombres = nombres.sorted(by: { (s1: String, s2: String) -> Bool in
    return s1 > s2
    })

let nombres = ["Anna", "Pedro", "Oscar", "Juan", "Carlos"]

func backward(_ s1: String, _ s2: String) -> Bool {
    return s1 > s2
}

var reservedNombres = nombres.sorted(by: backward)
//reservedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Anna", "Pedro", "Carlos", "Juan", "Oscar"]

reversedNombres = nombres.sorted(by: { (s1: String, s2: String) -> Bool in
    return s1 > s2
    })
//reversedNombres es igual a ["Pedro", "Oscar", "Juan", "Carlos", "Anna"]

let nombres = ["Pedro", "Juan", "Oscar", "Carlos", "Anna"]

reversedNombres = nombres.sorted(by: { (s1: String, s2: String) -> Bool in return s1 > s2 } )
//reversedNombres es igual 









